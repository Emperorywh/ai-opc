# 项目进度追踪（PROGRESS）

| 项 | 值 |
|---|---|
| 文档版本 | v1.0 |
| 日期 | 2026-06-16 |
| 配套文档 | SPEC（做什么）· ROADMAP（怎么做）· 本文件（做到哪了）|
| 维护者 | Claude Code（每 Task 完成时更新）+ 人工 Review |

> 本文件是「**当前做到哪了**」的**单一可信源**。每次新会话，agent 第一步读本文件定位当前任务。
> 三者关系：**SPEC** = 做什么 / 长什么样；**ROADMAP** = 用什么顺序做 / 每步边界与验收；**PROGRESS** = 做到哪了 / 踩了哪些坑。

---

## 当前指针

- **当前 Milestone**：M6 · 国家边界与描边（Phase 2 启动）
- **当前 Task**：Task 19 · 边界数据 pipeline
- **MVP 进度**：M1–M5 共 5 个 Milestone，已完成 **5 / 5**（✅ M1 地形沙盘 · ✅ M2 海洋与水彩质感 · ✅ M3 相机交互与自适应质量 · ✅ M4 大洲标签与中文字体 · ✅ M5 大气辉光·加载·署名）→ **Phase 1 MVP 代码闭环达成**（⚠️ 60fps / 4K / 截图集 M5.png / `git tag v0.1-mvp` 待人工 Review 后再打 tag）
- **总体进度**：32 个 Task，已完成 **19 / 32**

---

## Task 状态表

图例：⬜ pending ｜ 🔄 in_progress ｜ ✅ done ｜ ⚠️ blocked

### Phase 1 — MVP（M1–M5）

| Task | MS | 标题 | 状态 | Commit | 备注 |
|---|---|---|:---:|---|---|
| 01 | M1 | 项目基础设施与目录骨架 | ✅ | c1e5558 | 骨架：依赖+config四件套+store+空Scene |
| 02 | M1 | 合成 DEM Pipeline（免 GDAL） | ✅ | c4f1a5a | 自写PNG编解码+合成DEM；大陆可辨认；产出 heightmap/normal/meta |
| 02b | M1 | 真实 DEM 接入（GEBCO 2026） | ✅ | 94cce3c | 真实 GEBCO 产物已接入(4096×2048/min-10000/max9000)；闭环由人工下载+gen:dem:real 完成并提交(94f8988+f6bf8a6+94cce3c)，Task 05 补测验证 |
| 03 | M1 | 投影契约与数据加载层 | ✅ | 6f5cbcf | project()+高度解码契约(R3 同源)+16-bit PNG 加载(R32F)+CPU 高度表+vitest 20 测全绿 |
| 04 | M1 | GPU 顶点位移地形 + 基础着色 | ✅ | ef3f8cf | 自定义 ShaderMaterial：R32F heightmap 顶点位移(照搬 Task03 契约)+基础高度分层+Lambert 光照；PlaneGeometry 512×256；静态倾斜相机；25 测全绿 |
| 05 | M1 | M1 闭环验收 | ✅ | 4b406cb | 42测全绿(build/lint过)；真实DEM回归断言(11新)；dev视觉/console error 待人工 Review |
| 06 | M2 | 透明渲染顺序与海洋几何 | ✅ | 0d451e2 | Ocean 平面(同地形尺寸)铺海平面 y=metersToWorldY(seaLevel)；MeshBasicMaterial 半透明 oceanShallow(depthWrite=false/DoubleSide)+renderOrder=1；Scene 挂 Ocean(Terrain 先绘写深度→Ocean 后绘关深度写入)；10 新测断言渲染顺序契约/几何/海平面Y；52测全绿 build/lint 过 |
| 07 | M2 | Gerstner 海洋 shader | ✅ | a75b91e | oceanMaterial MeshBasic占位→自定义ShaderMaterial：Gerstner波(≤5,顶点位移+GPU Gems解析法线B/T→cross→N,N.y<0翻转)+菲涅尔pow(1-dot(N,V),3)掠射偏亮青绿(vViewDir vertex算,cameraPosition为three vertex-only内建)+深浅渐变(per-pixel同源heightmap水深-terrainY/uMaxDepth,浅#7FC4C0→深#2E6E73,vHeightUv用位移前世界坐标→海岸线稳定)+uTime驱动波相位流动；波数uWaveCount uniform开关(M2默认高档qualityConfigs[high].oceanWaves=5,≤0降正弦,M3改uniform不动shader)；透明渲染顺序契约(Task06#1)不退化；体积感由波振幅提供(海平面y=0不变,R3不破坏)；react-hooks/immutability禁useFrame mutate useMemo material→useRef+useEffect同步ref; oceanDepthFactor/oceanFresnel纯函数同源单测; 75测全绿(52→75)build(56mod 295ms)/lint过;⚠️dev视觉/GLSL编译待人工Review;M2 2/3 |
| 08 | M2 | 地形水彩 shader 完善 | ✅ | 6f85404 | terrainMaterial Task04 基础分层→完整水彩 shader：坡度强调(smoothstep 0.30-0.65 on 1-N.y 陡坡偏暖灰绿 仅陆地)+水彩噪声(hash fbm 4 频 ±10% 明度颗粒 无纹理依赖 仅陆地)+海岸线 fwidth 等高线(y≈0 1-smoothstep(0,fwidth·2,|y|) 导数抗锯齿描边 非额外几何)+软描边轮廓(pow(1-max(dot(N,V),0),2) 暖白 rim 仅陆地)+normal.png 细节增强(§6.1.3 Task04 注释 deferred→本Task：片元采样烘焙法线 编码 R=worldX/G=worldZ/B=worldY→vec3(r,b,g) 单位化 按权重 blend 进几何法线 dFdx/dFdy 治低频发软)；palette 完整接入(分层色硬编码hex→源自palette grassland/mountain/desert/snow/border + desaturateHex S降20%取中值 §2.1)；shader 复杂度开关(§8/D18 5 uniform 钩子 slope/noise/coastline/rim/detail TERRAIN_EFFECTS高档默认 createTerrainMaterial opts? per-toggle覆盖 M3改value不动shader)；既有契约不退化(shaderWorldY/TERRAIN_SEGMENTS/terrainLight导出未变 Terrain不透明先绘写深度 Lambert+sRGB保留)；纯函数同源单测(shaderSlopeFactor/shaderRimFactor/shaderCoastlineFactor/shaderDetailNormalDecode/desaturateHex)+shader源码正则防回归；palette.ts新增snow+desaturateHex纯TS HSL工具(palette原值不变 向后兼容 ocean未动)；46新测 共121测全绿(75→121) build(56mod 263ms)/lint过;⚠️dev视觉(水彩颗粒/坡度分层/海岸线/rim微光/normal细节实际观感)+浏览器console(GLSL编译/fwidth/normal采样)需人工Review;M2 3/3 ✅ |
| 09 | M3 | SandboxControls（受限 pan/zoom） | ✅ | fba5553 | StaticCamera(占位)→删; 新建 SandboxControls.tsx+cameraState.ts 球面状态机+纯函数约束(clampTarget pan x∈[-1.1,1.1]/z∈[-0.6,0.6]对齐PLANE 2×1外缘略放宽 / clampDistance zoom.min-max / clampPitch锁[45°,60°]任意输入含>90°翻背面/≤0°平视仰视全压回→永不翻转 / clampYaw锁主朝向0 / distanceToZoom远0近1归一化 / damp帧率无关 pow) + 拖拽pan(target拖地图方向,灵敏度distance·panSensitivity随距离缩放)+滚轮zoom(exp步长)+阻尼状态机 current→goal(useFrame damp+delta clamp≤0.1防后台回切); pitch锁45°/yaw锁0不绑输入→永不翻转; store cameraZoom节流(|Δ|>0.001)接入+挂载同步初始; camera.ts(Task01骨架)补panSensitivity0.0015/wheelZoomFactor0.001; computeCameraPosition pitch/yaw=0与Task04 StaticCamera同源(sinv·d/cosv·d)M1→M3无跳变; 既有契约不退化(渲染顺序/光照/store切片签名); 35测(pan不出界/pitch不翻转/同源静态相机/帧率无关阻尼两半帧≈一整帧)共156测全绿(121→156) build(60mod 335ms)/lint过;⚠️dev拖拽手感/pan方向直觉/zoom速率/阻尼跟手+console(pointer capture/wheel preventDefault)需Review;Task 10=InputAdapter抽象(鼠标vs触控板deltaY量级+触屏pinch预留)替换输入层,状态机/约束/阻尼不变; M3 1/3 |
| 10 | M3 | 输入手势适配 | ✅ | 434692c | InputAdapter 抽象层（SPEC §9/D20）:Task09 内置 pointer/wheel DOM 绑定→抽离; CameraInputHandlers 语义意图(onPan/onZoom)+InputAdapter 接口(attach→detach)+wheelToZoomFactor 纯函数(deltaMode 量级归一 LINE×16/PAGE×400 + ctrlKey pinch 独立系数 + Task09 PIXEL 回归不变量 exp(deltaY·wheelZoomFactor))+createMouseTrackpadAdapter(鼠标/触控板 pointer→pan/wheel→zoom,触屏单指 pan 天然覆盖)+createTouchAdapter(stub 预留 Phase2 多点); useCameraInput hook(src/hooks useState 懒初始化稳定 adapter+latest ref 透传 handlers+attach 全程一次); SandboxControls 删内联 useEffect 输入块→useCallback onPan/onZoom→useCameraInput,状态机/clamp/阻尼/useFrame/节流store 全不变(Task09 契约零退化); camera.ts +pinchZoomFactor 0.01; 27 测(fake-element 集成 node 无 DOM 不加依赖); ⚠️踩坑 react-hooks/refs render 期读 ref→改 useState 懒初始化 + DELTA_PIXEL 未用删除; 183 测全绿(156→183) build 62mod/lint 过; ⚠️dev 手感/触控板pinch/console 需 Review; M3 2/3 |
| 11 | M3 | AdaptiveQuality 分档 | ✅ | 2d43195 | AdaptiveQuality.tsx（FPS 滑窗+滞回状态机+useDetectGPU 初档）+qualityState 纯函数+quality.ts 分档(terrainEffects/adaptive 阈值)+store qualityTierOverride 手动覆盖；Ocean/Terrain 订阅 qualityTier 经工厂 opts 重建材质(同源 shader 仅改 uniform 值,兑现 M2 预留钩子)；38 新测共 221 全绿 build(601mod)/lint 过;⚠️dev 真实 FPS 降档/4K dpr/手动覆盖手感需 Review;M3 3/3 ✅ MVP 3/5 |
| 12 | M4 | 字体子集化 pipeline | ✅ | 62254e0 | charset.mjs（七大洲四大洋固定中文名+ASCII+标点 92字形）+ fetch-source.mjs（NotoSansSC OFL 源字体下载 GEBCO同款 raw/.gitignore）+ subset.mjs（subset-font harfbuzz 子集化 pin wght=400）→ public/fonts/map-zh.woff2 22.1KB(<100KB)；13测共234全绿 build/lint过；src/零改动前端消费留Task14 |
| 13 | M4 | 大洲/大洋标签数据 pipeline | ✅ | 7685d43 | labels-data.mjs 固定常识数据(CONTINENT/OCEAN_LABELS+buildLabels纯函数)+4-labels-zh.mjs CLI→labels.json 11条(7大洲P100+4大洋P80;{id,zhName,kind,continent,lon,lat,priority}对齐SPEC§6.5)；types.ts Label补continent:string|null(大洲=自身/大洋=null)；17测共251全绿 build 601mod/lint过；⚠️dev视觉留Task14 |
| 14 | M4 | LabelLayer（troika SDF） | ✅ | 0466338 | LabelLayer troika SDF：labelLayout(labelWorldPosition project+sampleWorldY max(地面,海面)+heightOffset/LABEL_STYLE 暖白描边字/labelFontUrl)+LabelLayer(每标签 Text 实例 primitive 挂载 sync+dispose)+assets parseLabels/loadLabels 校验+Scene 挂载(labels 独立加载不阻塞)+troika-three-text.d.ts ambient shim；17 测共 268 全绿 build 603mod/lint 过；⚠️dev troika 中文渲染/4K60fps/console 留 Review |
| 15 | M4 | 优先级视口碰撞 + LOD 联动 | ✅ | 51844be | collision.ts AABB 贪心剔除+densityVisible/zoomToDensity LOD 纯函数+useLabelCollision useFrame 节流投影设 visible+LabelLayer 接入+troika textRenderInfo 扩展；34 测共 302 全绿 build(605mod 423ms)/lint 过
| 16 | M5 | 大气层辉光（fresnel 弧壳） | ✅ | 7eee5d5 | atmosphereMaterial 扁椭圆上半球壳(BackSide+AdditiveBlending+depthTest=false view-space fresnel rim + atmosphereFresnel 纯函数 + ATMOSPHERE_BY_TIER 低档关辉光 + 椭圆 scale 2:1 贴合平面) + AtmosphereRim 订阅 qualityTier + Scene §4.3 末项挂载；19 测共 321 全绿 build(607mod)/lint 过 |
| 17 | M5 | 加载进度 + WebGL 检测降级 | ✅ | d1e5a30 | store loading 切片+loading.ts 分项进度纯函数(STAGE_PROGRESS/stageProgress/byteFraction/clamp01)+fetchWithProgress 流式字节进度+Loader DOM overlay 订阅渲染进度条/阶段/错误重试+webgl.ts detectWebGL 注入式 WebGL2/1 检测+WebGLFallback 内联 SVG 静态预览+Scene 加载编排上报+App WebGL 门控挂载；35 测共 356 全绿 build(611mod)/lint 过 |
| 18 | M5 | 数据署名 + 许可弹窗 + MVP 验收 | ✅ | e7840fd | credits.ts 纯数据(DATA_SOURCES GEBCO 2026+Noto Sans SC OFL + CATEGORY_LABELS/ORDER + FONT_LICENSE_NOTE + isValidSource/activeSources/formatAttributionLine/groupSourcesByCategory 纯函数)+Hud.tsx 左下角常驻署名+许可弹窗(ESC/点遮罩关 z-index 与 Loader 解耦 pointer-events:none 不挡 SandboxControls)+App 挂载；27 测共 383 全绿 build(613mod 348ms)/lint 过；MVP 代码闭环；⚠️60fps/4K/M5.png/git tag v0.1-mvp 待人工 Review |

### Phase 2 — 国家边界与交互（M6–M9）

| Task | MS | 标题 | 状态 | Commit | 备注 |
|---|---|---|:---:|---|---|
| 19 | M6 | 边界数据 pipeline | ⬜ | — | — |
| 20 | M6 | CountryMeshes + BorderLines | ⬜ | — | — |
| 21 | M6 | 争议虚线 | ⬜ | — | — |
| 22 | M7 | 拾取 RT + ID 颜色映射 | ⬜ | — | — |
| 23 | M7 | hover/selected 高亮 + hook | ⬜ | — | — |
| 24 | M8 | 数据标注面板 | ⬜ | — | — |
| 25 | M8 | 国家中文标签 + Legend | ⬜ | — | — |
| 26 | M9 | Robinson 投影实现 + 重投影 pipeline | ⬜ | — | — |
| 27 | M9 | 全矢量对齐验证 | ⬜ | — | — |

### Phase 3 — 河流与增强（M10–M11）

| Task | MS | 标题 | 状态 | Commit | 备注 |
|---|---|---|:---:|---|---|
| 28 | M10 | 河流数据 pipeline | ⬜ | — | — |
| 29 | M10 | 流动发光河流 shader | ⬜ | — | — |
| 30 | M11 | 触屏输入接入 | ⬜ | — | — |

### Phase 4 — 可选演进（M12）

| Task | MS | 标题 | 状态 | Commit | 备注 |
|---|---|---|:---:|---|---|
| 31 | M12 | 四叉树流式 LOD | ⬜ | — | — |

---

## Milestone 完成度

| MS | 名称 | Task 完成 | 状态 |
|---|---|---|---|
| M1 | 地形沙盘地基 | 6/6 | ✅ | 含 Task 02b 真实 DEM（GEBCO 2026）|
| M2 | 海洋与水彩质感 | 3/3 | ✅ |
| M3 | 相机交互与自适应质量 | 3/3 | ✅ |
| M4 | 大洲标签与中文字体 | 4/4 | ✅ |
| M5 | 大气辉光·加载·署名（MVP 闭环） | 3/3 | ✅ |
| M6 | 国家边界与描边 | 0/3 | ⬜ |
| M7 | GPU 颜色拾取与交互高亮 | 0/2 | ⬜ |
| M8 | 数据标注面板与国家标签 | 0/2 | ⬜ |
| M9 | 投影升级 Robinson | 0/2 | ⬜ |
| M10 | 河流系统 | 0/2 | ⬜ |
| M11 | 触屏输入与视觉增强 | 0/1 | ⬜ |
| M12 | 四叉树流式 LOD（可选） | 0/1 | ⬜ |

---

## 近期注意事项（Lessons Learned）

> 每个 Task 完成后在此追加 1–2 行踩坑 / 关键决策，供后续会话参考。**倒序**（最新在上）。

- **Task 18（2026-06-17）**：数据署名 + 许可弹窗 + MVP 验收（M5 末 Task，**Phase 1 MVP 代码闭环**）。落地 SPEC §6.7「角落常驻数据来源署名 + 许可弹窗（合规）」+ §12 数据来源规划 + §4.2「UI 与 3D 通过 store 解耦」（本 Task 用 z-index 解耦，无需 store）。**两文件新增 + 一文件改 + 一测试新增**：①**`src/ui/credits.ts`**（核心，纯数据+纯函数，照 loading.ts/atmosphereMaterial/labelLayout 同构「非组件模块承载逻辑、组件只导出组件满足 react-refresh」）—— `DataSource` 接口 `{id,name,role,category,license,url,active}` + `SourceCategory='terrain'|'font'` + `CATEGORY_LABELS`(terrain→地形数据/font→字体) + `CATEGORY_ORDER` + `DATA_SOURCES`（MVP active 两条）+ `FONT_LICENSE_NOTE`(OFL 子集化仍需保留许可声明) + **纯函数** `isValidSource`(字段非空+url http(s) 正则)/`activeSources`(filter active)/`formatAttributionLine`(来源名「·」连接成署名行)/`groupSourcesByCategory`(按 CATEGORY_ORDER 分组保序跳空)。**关键决策（署名内容，SPEC 为准）**：ROADMAP Task 18 行「NE/Copernicus/REMA」是数据源家族**模板名**；SPEC §12.1 明确 **MVP 实际地形=GEBCO 2026**（Task 02b 已打包 4096×2048），未接入的 NE 国家边界/河流留 Phase 2/3、Copernicus/REMA 留 Phase 4。**合规原则只署名实际打包/分发的资产** → MVP 署名 = **GEBCO 2026 Grid（地形，免费公开数据建议署名，gebco.net）+ Noto Sans SC/思源黑体（字体，SIL OFL 1.1，Task 12 子集化）**。`active:false` 预留位备 Phase 2/3 接入后翻 true（数据结构就绪不破坏测试）。GEBCO 许可措辞取「免费公开数据（建议署名）」而非断言「公共域」（GEBCO 实为 free-of-charge license，措辞留余量；准确度优先）。②**`src/ui/Hud.tsx`**（R3F 外 DOM overlay 组件）—— **常驻署名行**（左下角胶囊：`数据：{formatAttributionLine}` + 暖白「许可」按钮 setOpen(true)，palette.border/oceanShallow 低饱和，glass blur 半透明背景）；**许可弹窗 LicenseModal**（子组件，role=dialog/aria-modal：居中卡片按分类分组列出来源 `SourceRow`(名称外链 target=_blank + 许可徽标 + 用途说明) + OFL 附加说明 + 关闭按钮；**ESC 关闭** useEffect keydown 仅 open 时挂监听 / **点遮罩关闭** onClick=onClose + 卡片 stopPropagation）。③**`src/App.tsx`** 挂 `<Hud/>` 于 `<Loader/>` 后。**z-index 解耦（关键，非 store）**：加载期 Loader（z50 全屏不透明 rgba0.94）**遮蔽** Hud(z30)，ready 后 Loader 自卸载、Hud 显现；弹窗 z60 浮最上。**Hud 纯展示、不订阅 store**（署名是静态资产信息、弹窗开关是纯 UI 局部 useState，无需跨 Canvas 内外桥梁——与 Loader 的 loading 切片不同）。**pointer-events:none 关键**：Hud 容器 `pointer-events:none`（overlay 满屏但不拦截），仅可交互元素（署名按钮/弹窗）`pointer-events:auto` 重开，**避免遮挡 SandboxControls 拖拽 pan/滚轮 zoom**（DOM overlay 叠 3D canvas 标准实践）。**既有契约不退化**：渲染顺序/光照/投影/store 切片/SandboxControls/AdaptiveQuality/Loader/WebGLFallback 全保留；Hud 纯新增 UI 叠加层不改 three/data/scripts 主体。**模块数 611→613**（+Hud/credits；react/three/palette 已引入不新增依赖图）；bundle gzip 360→361KB（+~1KB 轻量纯 TS + 内联样式）。**无踩坑**（react-refresh：Hud.tsx 仅导出 Hud 组件，LicenseModal/SourceRow 为非导出子组件；react-hooks：useMemo/useCallback/useEffect 依赖完整；import type 类型擦除不触发 react-refresh）。**验收**：credits 纯函数全单测（DATA_SOURCES 字段完整/id+url 唯一/category 合法枚举/MVP=GEBCO+NotoSansSC OFL/MVP 不误列 NE/Copernicus/REMA/activeSources 过滤/formatAttributionLine「·」连接/分组保序跳空/isValidSource 各非法分支）✅ ｜ build(613mod 348ms)/lint 过 ✅ ｜ **383 测全绿（356→383，+27）** ✅。⚠️ **dev 视觉 + MVP 终验留人工 Review**（agent 无浏览器，Task 惯例 + 本 Task 为 MVP 收官）：① Hud 常驻署名行实际观感（左下角胶囊是否克制不抢戏、低饱和暖白是否与水彩协调、长名省略号截断）② 许可弹窗（点「许可」打开、ESC/点遮罩/关闭按钮关闭、外链跳转、分组排版、OFL 说明）③ pointer-events 不挡相机（拖拽/缩放是否顺滑、署名行区域是否仍可点按钮）④ z-index 叠层（加载期 Hud 被 Loader 遮蔽、ready 后显现、弹窗浮最上）⑤ **MVP 终验（SPEC §11）**：桌面 60fps / 4K 不爆显存 / 七大洲四大洋清晰可缩放平移标签不重叠 / 风格符合 50%动漫+30%图鉴+20%科技 ⑥ 浏览器 console（Hud/弹窗无 error）⑦ 截图集 `docs/screenshots/M5.png`（M5 三 Task 视觉汇总：辉光+加载+署名）⑧ 性能报告。**`git tag v0.1-mvp` 暂不打**：tag 是 MVP 验收声明（含 60fps/4K/视觉），agent 无法验证 → 留人工 Review 通过后由用户执行（`git tag v0.1-mvp`，可经 `!` 前缀在本会话跑）。**M5 收官：3/3 ✅，Phase 1 MVP 代码闭环 5/5 ✅（总体 19/32）**。下一 Task：M6 Task 19 边界数据 pipeline（Phase 2 启动，需 mapshaper/proj4/earcut，Natural Earth Shapefile）。

- **Task 17（2026-06-17）**：加载进度 + WebGL 检测降级（M5 第二 Task）。落地 SPEC §加载体验「Loader 分项进度（drei useProgress **或自研**）」+ §13.5「WebGL2 假定需检测降级」+ §10「WebGL 不支持 → 静态预览图 + 提示」+ §4.2「UI 与 3D 通过 store 解耦」。**四文件新增 + 三文件改 + 两测试新增**：①**`src/state/store.ts`**（改，+loading 切片）—— `LoadingStage` 类型（init/meta/terrain/decode/ready 单调递进）+ `loadingStage`/`loadingProgress(0-1)`/`loadingError` 三字段 + `setLoading(stage,progress)`（progress 内联 clamp 到 [0,1]，避免 store→ui 依赖）/`setLoadingError(msg)`。**跨边界决策（声明）**：加载状态横跨 UI 层（Loader，Canvas 外 DOM overlay）与 3D 层（Scene，Canvas 内），R3F `<Canvas>` 创建独立 reconciler root **不继承外部 React Context**（drei 须 Bridge 桥接），而 zustand store 是单例、Canvas 内外皆可直接 `useStore` → store 是唯一干净桥梁（SPEC §4.2 设计意图 + Task 11 AdaptiveQuality「Task 加 store 切片」既有模式）。store.ts 不在 M5 禁止列表（禁止的是 src/three 子系统主体/src/data/scripts），纯新增切片、零现有契约破坏。②**`src/ui/loading.ts`**（新，纯常量+纯函数+IO 工具，照 atmosphereMaterial/collision/labelLayout 同构「非组件模块承载逻辑、组件只导出组件满足 react-refresh」）—— **为何自研不用 drei useProgress**：资源加载不走 R3F DefaultLoadingManager——heightmap.png/labels.json 用原生 `fetch`、troika 字体 troika 自加载，仅 normal.png 经 TextureLoader，useProgress 无法完整跟踪（§加载体验明确「useProgress **或**自研」）→ 自研分项进度。`STAGE_PROGRESS`（各阶段整体进度区间 [start,end]：init 0-0.02 / meta 0.02-0.06 / terrain 0.06-0.88 **主体** / decode 0.88-0.97 / ready 1.0）+ `STAGE_LABELS`（中文阶段文案）+ `STAGE_ORDER` + `isReady` / `clamp01`（非有限→0）/ `byteFraction`（防 total≤0/非有限→0）/ `stageProgress(stage,fraction)`（阶段内比例→整体线性插值，Scene 调用写 store）+ **`fetchWithProgress(url,onProgress)`** 流式 fetch：读 `res.body.getReader()` 逐块累加字节回调 `onProgress(loaded,total)`；无 content-length / 无 ReadableStream（不透明响应）时回退整体 arrayBuffer（首尾各回调一次）；非 2xx 抛错由 Scene catch 写 store.loadingError。heightmap 最大（11MB+）→ 字节级进度最有感，驱动 terrain 阶段 0→1；normal（TextureLoader 无字节回调）/decode 瞬时给固定权重。③**`src/ui/webgl.ts`**（新，纯函数，依赖注入可单测）—— `detectWebGL(makeCanvas?)` → `{webgl2, webgl, supported}`；优先 WebGL2，可用即不再探测 WebGL1（three 0.184 用 WebGL2，避免多余 getContext 调用）；**依赖注入 makeCanvas**：Node 单测无 document → 注入 mock canvas factory 验证判定逻辑；运行时默认 `document.createElement('canvas')`，无 document（SSR）安全回退 unsupported；getContext 抛错（WebGL 被禁用）/ factory 抛错（无 DOM）均 catch → unsupported 不崩溃。④**`src/ui/Loader.tsx`**（新，DOM overlay 组件）—— 订阅 store loadingStage/Progress/Error；**ready 且无 error → return null 卸载**（Canvas 已可见）；否则全屏 overlay（rgba(14,16,20,0.94)+backdrop-blur，与 Scene 背景 `#0e1014` 协调）：标题「世界沙盘」+ 进度条（track + fill `width=progress%` CSS transition 平滑跟进 + 流光 shimmer 动画）+ 阶段文案 STAGE_LABELS（pulse 呼吸）+ 百分比；错误态（role=alert）显示 loadingError + 「重新加载」按钮（`window.location.reload`）。**样式自包含**（内联 style + 一次性 `<style>` keyframes），不改全局 index.css 守 M5 边界；低饱和水彩色取 palette（border 暖白 / oceanShallow 青绿，§2.1）。⑤**`src/ui/WebGLFallback.tsx`**（新，降级组件）—— App 检测无 WebGL 时替代 Canvas；内联 SVG 简化世界地图（2:1 viewBox 400×200，七大洲简化椭圆/矩形色块，经纬度粗映射非精确地理仅"可辨认"占位；海洋 oceanDeep→oceanShallow 渐变 + 陆地 grassland/mountain/desert/snow 色）+ 中文提示「您的浏览器不支持 WebGL / 建议升级支持 WebGL2 的现代浏览器或启用硬件加速」。**agent 无浏览器无法生成真实地图截图 → 自包含 SVG 占位**（降级路径 detectWebGL + 组件纯函数可测），真实预览图留人工 `pnpm dev` 截图替换。⑥**`src/three/Scene.tsx`**（改，加载编排上报）—— 原 `loadTerrainAssets()` 单一 Promise（无内部阶段）→ **细粒度编排上报 store**：用 data 层导出函数 `loadMeta`/`loadNormalTexture`/`decodeHeightmap`/`createHeightTexture`（**仅 import 不改 src/data**）+ `fetchWithProgress(HEIGHTMAP_URL)` 自行编排 init→meta→并行[heightmap 字节进度（terrain 0→1）+ normal 静默]→decode→texture→ready，各步 `useStore.getState().setLoading(stage, stageProgress(stage,fraction))`；catch → setError + console.error + `store.setLoadingError`；**labels useEffect 不变**（Task 14 独立加载不阻塞不报进度）；loadTerrainAssets 函数保留不删（向后兼容）。⑦**`src/App.tsx`**（改）—— `useMemo(()=>detectWebGL(),[])` 运行一次；`!supported` → `<WebGLFallback/>`；否则 `<Canvas><Scene/></Canvas>` + `<Loader/>`（DOM overlay，Canvas 外，订阅 store；ready 自卸载）。**踩坑（浮点累积误差）**：`stageProgress('terrain',1)=0.06+0.82*1=0.8800000000000001`（JS 浮点加法）≠ `STAGE_PROGRESS.terrain[1]=0.88` 字面量 → 测试 fraction=1 / 超界 断言改 `toBeCloseTo(...,10)`（fraction=0 / ready 精确 toBe 不受影响：0.06+0.82·0=0.06、ready 直接 1.0）。**既有契约不退化**：渲染顺序（Terrain 先绘/Ocean 后绘/LabelLayer/AtmosphereRim）/光照/投影/store cameraZoom + qualityTier 切片/SandboxControls/AdaptiveQuality 全保留；store 仅纯新增 loading 切片不改现有；Scene 渲染主体（assets→Terrain/Ocean/LabelLayer）逻辑不变，仅加载 useEffect 加上报；App Canvas/Scene 挂载不变仅加 WebGL 门控 + Loader 叠加。**模块数 607→611**（+loading/webgl/Loader/WebGLFallback；store/Scene/App 已存在；three/R3F/zustand/palette 已引入不新增依赖图）；bundle gzip 356→360KB（+~3KB Loader/Fallback/loading/webgl 轻量纯 TS + 内联 SVG）。**验收**：分项进度纯函数全单测（stageProgress/byteFraction/clamp01/isReady/STAGE_PROGRESS 单调递增/terrain 占主体跨度最大）✅ ｜ fetchWithProgress 流式字节进度 mock（分块回调 + 合并 buffer / 单字节边界 / 非 2xx 抛错 / 无 stream 回退 / 无 content-length 回退）✅ ｜ detectWebGL 注入 mock canvas（WebGL2 / 仅 WebGL1 / 都不支持 / null / getContext 抛错 / factory 抛错 / WebGL2 可用不重复探测 / 无 document 回退）✅ ｜ build（611mod 409ms）/lint 过 ✅ ｜ **356 测全绿（321→356，+35：loading 27 + webgl 8）** ✅。⚠️ **dev 视觉留人工 Review**（agent 无浏览器，Task 惯例）：① Loader 实际观感（进度条平滑跟进 / 阶段文案切换 / 百分比 / 流光 + 呼吸动画是否克制不抢戏、低饱和暖白是否与水彩协调）② heightmap 字节进度真实推进感（11MB 文件下载是否逐 chunk 推进、normal 并行不回退进度）③ ready 后 Loader 是否干净卸载（Canvas 可见无遮挡）④ 错误态（断网 / 404 触发 loadingError 显示 + 重新加载按钮）⑤ WebGL 降级（DevTools 关闭 WebGL 或旧浏览器验证 WebGLFallback SVG + 提示）⑥ 浏览器 console（ReadableStream 读取 / detectWebGL / 无 error）⑦ 4K 60fps；`docs/screenshots/M5.png` 待 Task 18 后归档（M5 三 Task 视觉汇总：辉光 + 加载 + 署名）。**M5 进度：2/3**。

- **Task 16（2026-06-17）**：大气层辉光 fresnel 弧壳（M5 首 Task）。落地 SPEC §6.7「贴合倾斜平面沙盘边缘的背面弧壳 / 平面边缘 fresnel 边框（二选一，取前者）+ additive + fresnel」+ §4.3 渲染管线末项 + §13「不引入全局 Bloom，用 fresnel shell」+ §8「低档关辉光」。**两文件新增 + 一文件改 + 一 .gitkeep 删 + 一测试新增**：①**`src/three/atmosphere/atmosphereMaterial.ts`**（核心，纯常量+纯函数+GLSL+工厂，照 oceanMaterial.ts 同构「非组件模块承载，组件只导出组件满足 react-refresh」）—— **几何**：扁椭圆**上半球壳**（`SphereGeometry(R=1.0, 64, 32, 0, 2π, 0, π/2)` thetaLength=π/2 取北半球）+ `mesh.scale=[SHELL_SCALE_X=1.18, SHELL_FLATTEN=0.12, SHELL_SCALE_Z]`；**关键**：PLANE 是 2:1（宽 2 纵 1）非正方形，圆形球壳会纵向溢出过多 → `SHELL_SCALE_Z = SHELL_SCALE_X × (PLANE_HEIGHT/PLANE_WIDTH)` 保持 **2:1 比例**，椭圆壳边缘精确贴合长方形平面外缘（`SHELL_SCALE_X>PLANE_WIDTH/2` 留边缘辉光余量）；SPEC §6.7 明确「**曲面而非整球**」（平面沙盘非地球仪），上半球壳压扁 = 大曲率扁弧壳（曲面），非整球。**材质契约**：`BackSide + AdditiveBlending + transparent + depthWrite=false + depthTest=false`（导出 plain object 供单测）；**depthTest=false 决策**：辉光壳罩地形上方，depthTest=true 会被地形写深度部分遮挡致光晕断裂；fresnel 使壳顶（正视）rim≈0 不可见、仅掠射边缘 rim 大发光 → depthTest=false 不导致中心过亮，光晕始终完整叠加（与大气辉光壳标准做法一致，drei atmosphere 教程同模式）。**shader**：view-space fresnel（`normalMatrix` 含 inverse-transpose 正确处理非均匀缩放；fresnel 只依赖 N·V 角度与空间无关）+ `abs(dot(N,V))` 兼容 BackSide 法线朝向（朝地图内）+ `pow(clamp(1-abs(dot),0,1), uFresnelPower)` rim + 末尾 gamma `pow(col,1/2.2)`（raw ShaderMaterial 不自动 sRGB encode，与 ocean/terrain 一致）+ AdditiveBlending src·a+dst。**纯函数** `atmosphereFresnel(nDotV,power)`（正视±1→0/掠射0→1/abs 对称/单调/clamp/power 越大边缘越窄，与 GLSL 同源全单测）。**质量分档** `ATMOSPHERE_BY_TIER`(high=0.6/medium=0.39 启用·low 关闭 intensity=0) —— **边界决策**：SPEC §8「低档关辉光」但 `quality.ts`（src/config）**不在 Task 16 允许边界**（仅 `src/three/atmosphere/**`+Scene），故分档映射放 atmosphereMaterial **模块内自包含**（与 oceanWaves/terrainEffects 同模式仅配置位置不同），避免越界改 quality.ts 集中配置。**辉光色** `desaturateHex('#9FC4D2')` 冷青白（§2.1 低饱和，与水彩暖调协调）。**渲染顺序** `ATMOSPHERE_RENDER_ORDER=10` > Ocean(1)，§4.3 管线末项最后绘叠加。②**`src/three/atmosphere/AtmosphereRim.tsx`**（R3F 组件）—— 订阅 `useStore(qualityTier)` 读 `ATMOSPHERE_BY_TIER` cfg；`useMemo(createAtmosphereMaterial(cfg.intensity),[cfg.intensity])` 同源 shader 仅 uniform 值变（同 Ocean waveCount 模式）；`cfg.enabled===false`(低档) → `return null` 不渲染省片元开销（hooks 在 early return 前调用合规）；挂 `<mesh scale>` + `<sphereGeometry args=[R,64,32,0,2π,0,π/2]>`（上半球壳）；**不依赖 assets**（辉光装饰层不读 heightmap），Scene 数据加载前后皆可渲染。③**`src/three/Scene.tsx`** 挂 `<AtmosphereRim/>` 于 assets 块之后（§4.3 末项，renderOrder 保绘制序，挂载位置 scene root 无关）。④删 Task01 占位 `src/three/atmosphere/.gitkeep`。⑤**`test/atmosphere.test.ts`** **19 测**（atmosphereFresnel 6：正视±1→0/掠射0→1/|N·V|单调递减/abs 对称 BackSide 鲁棒/clamp 超界/power 越大边缘越窄 ｜ 几何常量 5：辉光色合法 hex/desaturateHex 产出/fresnelPower+强度正且≤1/椭圆 scale 2:1 scaleZ/scaleX=PLANE_H/PLANE_W/壳覆盖平面半宽/半径压扁细分 theta=π/2 ｜ 材质契约 4：OPTS(additive/BackSide/depthWrite/depthTest)+createAtmosphereMaterial 输出 ShaderMaterial+uniform+GLSL 正则(normalMatrix/abs(dot)/uFresnelPower)+默认强度+renderOrder>OCEAN_RENDER_ORDER ｜ 质量分档 4：低档关/高档启且最大/中档弱于高档/三档齐全）。**既有契约不退化**：渲染顺序（Terrain 先绘/Ocean 后绘/LabelLayer）/光照/投影/store qualityTier 切片/SandboxControls/AdaptiveQuality 全保留；AtmosphereRim 纯新增叠加层不改 terrain/ocean/labels/camera/data/scripts 主体。**模块数 605→607**（+atmosphereMaterial/AtmosphereRim；three/R3F 已引入不新增依赖图）；bundle gzip 356→357KB（+~1KB 纯 additive 壳 shader 极轻）。**无踩坑**（react-hooks 规则均满足：material useMemo dep intensity 稳定 / 无 useFrame / 无 mutate ref）。**验收**：fresnel 纯函数 6 测同源 ✅ ｜ 材质契约（additive/BackSide/depthWrite=false/depthTest=false）✅ ｜ 椭圆 scale 2:1 贴合平面边缘 ✅ ｜ 低档关辉光 ✅ ｜ renderOrder 高于 Ocean ✅ ｜ build(607mod 674ms)/lint 过 ✅ ｜ **321 测全绿（302→321，+19）** ✅。⚠️ **dev 视觉留人工 Review**（agent 无浏览器，Task 惯例）：① fresnel 边缘光晕实际观感（扁椭圆壳边缘是否贴合 2:1 平面、柔和光晕不抢戏不破坏水彩美学，SPEC 风险验证#1）② additive 叠加亮度/颜色（冷青白是否与水彩暖调协调、强度 0.6 是否克制）③ 俯瞰 45° 光晕形态（扁椭圆壳是否合理、壳顶 rim≈0 是否真不可见只边缘亮）④ depthTest=false 是否致中心区域意外过亮（fresnel 抑制是否足够）⑤ low tier 关辉光（`useStore.getState().setQualityTierOverride('low')` 验证光晕消失、medium 减弱）⑥ 浏览器 console（GLSL 编译/BackSide 渲染/additive 混合/无 error）⑦ 4K 60fps；`docs/screenshots/M5.png` 待 Task 17/18 后归档（M5 三 Task 视觉汇总：辉光+加载+署名）。**M5 启动：1/3**。

- **Task 15（2026-06-17）**：优先级视口碰撞 + LOD 联动（M4 末 Task，M4 收官）。落地 SPEC §6.5.1–5「优先级视口碰撞剔除 + LOD 缩放联动」+ §8「碰撞节流每 2~4 帧」+ §8 quality tier `labelDensity`。**两文件新增 + 两文件改 + 一测试新增**：①**`src/three/labels/collision.ts`**（核心，纯常量+纯函数，可脱离 three/troika/DOM 全单测，照 labelLayout.ts/oceanMaterial.ts/cameraState.ts 同构「非组件模块承载算法、组件/hook 只做胶水」）—— **碰撞**：`AABB{minX,minY,maxX,maxY}` + `aabbIntersect`（**严格相交**，共享边/角不算，避免紧贴判重叠）+ `greedyCollision(PlacedLabel[])`（按 priority **降序**贪心放入，与已放置 AABB 相交则剔除，返回应显示 id 集合；同级稳定 sort；O(n²) 数百标签可承受，上千换 Quadtree）+ `aabbFromCorners`/`ndcToScreen`(NDC→像素 Y 翻转)/`padAabb`+`LABEL_PADDING_PX=4`(外扩间距)。**LOD**：`DENSITY_VISIBILITY`(all=2/major=1/continent=0 可见度) + `KIND_MIN_VISIBILITY`(continent=0/ocean=1/country=1/city=2 最低显示密度，落地 §6.5.1「大洲>大洋>大国>小国」梯度) + `densityVisible(kind,density)`(可见度≥需求→显示) + `stricterDensity(a,b)`(取更严格者，zoom LOD × qualityTier 协同) + `LOD_ZOOM_THRESHOLDS`(continent 0.33/major 0.66) + `zoomToDensity(zoom∈[0,1])`(远→continent 仅大洲/近→all 全部)。②**`src/hooks/useLabelCollision.ts`**(R3F useFrame 胶水，ROADMAP M4 允许 src/hooks/**) —— `COLLISION_INTERVAL_FRAMES=3` 节流(§8「每 2~4 帧」) + `projectTextAabb`(troika `textRenderInfo.visibleBounds`[minX,minY,maxX,maxY] 局部世界单位 4 角 → `text.updateMatrixWorld()` → matrixWorld 局部→世界 → `camera.project` 世界→NDC → ndcToScreen 像素 → aabbFromCorners；文字面朝+Z 俯瞰45° 取 4 角保守包围 AABB 符合 SPEC「视口 AABB」) + useFrame 三步(①收集 LOD 可见+已 sync 候选 AABB ②greedyCollision ③设 visible：LOD 不达→false/候选→碰撞结果/未 sync→保留) + store `useStore.getState()` 非订阅读 cameraZoom/qualityTier(避每帧 re-render，同 Task 11 AdaptiveQuality) + texts/labels 经 `useRef`+`useEffect` 同步(避 react-hooks/immutability 对 useMemo 返回值 mutate 误报 + react-hooks/refs render 期读禁令，同 Ocean matRef 模式)。③**`src/three/labels/LabelLayer.tsx`** 改造：Task 14 `TroikaLabel` 子组件 → **内联到 LabelLayer 集中持有 `texts:Map<id,Text>`**(useMemo 构造期设属性+sync，primitive 挂载，useEffect cleanup dispose，`useLabelCollision(texts,labels)` 接入)；Task 14 渲染契约不退化(锚点 labelWorldPosition/fontUrl/LABEL_STYLE/troika sync+dispose 全保留)。④**`src/types/troika-three-text.d.ts`** 扩展：Text 加 `textRenderInfo:{blockBounds;visibleBounds}|null`(Task 14 建的 ambient shim 补碰撞所需字段；troika 0.52 dist 确认 visibleBounds=[minX,minY,maxX,maxY] 局部世界单位，sync 完成前 null)。⑤**`test/collision.test.ts`** **34 测**(aabbIntersect 7：部分/包含/被包含/共享边角严格/对称 ｜ greedyCollision 7：无重叠全留/全重叠留最高/高优先保留低让位独立留/降序放置/空输入/同级稳定/纯函数不改入参 ｜ aabbFromCorners 2 ｜ ndcToScreen 4 含 Y 翻转 ｜ padAabb 3 ｜ densityVisible 4 kind×3 density 矩阵 ｜ stricterDensity 3 ｜ zoomToDensity 4 含阈值单调)。**范围切割兑现**：Task 15 = collision.ts 算法纯函数(全单测) + useLabelCollision R3F 胶水(dev Review) + LabelLayer 接入；M4 11 条标签地理分散几无重叠(Task 14 备注已断言)，碰撞在 MVP 阶段主要**验证算法正确性**为 Phase 2 数百标签铺路(§6.5.5 排序列表足够)。**LOD 联动语义(关键设计)**：store 默认 cameraZoom=1(最近)+qualityTier=high → density=all → 11 条全显(**与 Task 14 行为一致，无回归**)；zoom out 至 zoom<0.33 → continent 密度 → 大洋隐藏仅 7 大洲(§6.5.4)；AdaptiveQuality 降 low tier → labelDensity=continent → 同样仅大洲(§8)；zoom LOD 与 qualityTier 经 stricterDensity 取严协同。**troika bounds 投影**：文字面朝+Z 在俯瞰45°下是竖直平面，4 角投影屏幕 AABB 是保守包围(非精确 OBB)，符合 SPEC「视口 AABB」；`updateMatrixWorld()` 确保锚点/group 变换已应用(useFrame 时机 three 自动更新世界矩阵可能滞后一帧)。**既有契约不退化**：渲染顺序(Terrain 先绘/Ocean 后绘)/光照/投影/store cameraZoom 切片/SandboxControls/AdaptiveQuality/Task 14 渲染层(锚点/字体/样式)全保留；troika Text transparent 物体渲染序不变；**Scene.tsx 未动**(LabelLayer props 接口 assets+labels 不变，碰撞在内部完成，守 M4 允许边界)。**模块数 603→605**(+collision/useLabelCollision；three/R3F 已被 Task 04/09/11 引入不新增依赖图)；bundle 1.24MB 持平/gzip 356KB(+~1KB 纯算法)。**踩坑(react-hooks 双规则)**：texts Map 是 useMemo 返回值 → useFrame 内 mutate `text.visible` 会触发 `react-hooks/immutability` → 用 `useRef`+`useEffect` 同步 textsRef/labelsRef，useFrame 读 `ref.current.get(id)`(同 Ocean matRef 模式，Task 07 踩坑经验)。**验收**：collision 纯函数全单测(aabbIntersect/greedyCollision/densityVisible/zoomToDensity/ndcToScreen/padAabb/aabbFromCorners)✅ ｜ build(605mod 423ms)/lint 过 ✅ ｜ **302 测全绿(268→302，+34)** ✅。⚠️ **dev 视觉留人工 Review**(agent 无浏览器，Task 惯例)：① 碰撞剔除实际观感(11 条基本不重叠；可制造重叠如拉近两个大洲锚点验证贪心隐藏低优先) ② LOD 联动(zoom out 大洋标签是否隐藏仅剩大洲、zoom in 恢复) ③ qualityTier=low 手动覆盖大洋是否隐藏 ④ 节流 3 帧是否有可见闪烁(visible 切换延迟) ⑤ troika bounds 投影 AABB 是否合理(文字屏幕包围盒、45° 竖直面保守包围) ⑥ 浏览器 console(troika textRenderInfo 可用/无 error) ⑦ 4K 60fps；`docs/screenshots/M4.png` 待人工 `pnpm dev` 归档(M4 四 Task 视觉汇总：字体子集+中文标签+碰撞+LOD)。**M4 收官：4/4 ✅，MVP 4/5**。

- **Task 14（2026-06-17）**：LabelLayer troika SDF 标签渲染层（M4 第三 Task）。落地 SPEC §6.5「默认 troika-three-text，每标签独立 Text 实例」+ §4.3 渲染管线（LabelLayer 在 Ocean 之后绘制）。**四文件新增 + 两文件改 + 一 .gitkeep 删**：①**`src/three/labels/labelLayout.ts`**（核心，纯常量+纯函数可脱离 three/troika 单测，照 oceanMaterial.ts/terrainMaterial.ts 同构「非组件模块承载常量/函数、组件只导出组件满足 react-refresh」）—— `LABEL_STYLE`（视觉样式集中：fontSize 0.07 ｜ color #F3E9D2 暖白取 palette.border 同源 ｜ outlineWidth 0.006 / outlineColor #1a1d22 深描边 / outlineOpacity 0.95 动漫图鉴风描边字 ｜ anchorX-Y center-middle ｜ heightOffset 0.012 浮起；**美学留 Review**，集中便于调）+ `labelFontUrl()`（`${BASE_URL}fonts/map-zh.woff2` 懒求值，同 dataUrl 模式）+ **`labelWorldPosition(label,elevation,meta,yOffset)`** 锚点（SPEC §6.5「project+地形高度查询 y」：x,z=`project(lon,lat)` R2 同源 ｜ **y=max(sampleWorldY, seaLevelWorldY)+yOffset**）。②**`src/three/labels/LabelLayer.tsx`**（R3F 组件）—— `LabelLayer` 映射 labels→`TroikaLabel` 子组件（fontUrl useMemo 懒求值）+ `TroikaLabel` 每标签独立 `new Text()`（useMemo 构造期设 text=zhName/font/style 全套 + `labelWorldPosition` 锚点 `position.set` + `sync()` 异步 SDF 生成 ｜ `<primitive object={t}>` 挂载 ｜ useEffect cleanup `dispose()` 释放 GPU）；troika 默认固定向上（非 billboard，俯瞰+45° 文字竖直，§6.5「billboard 或固定向上」取后者）。③**`src/types/troika-three-text.d.ts`**（**ambient 类型 shim**，同 Task03 fast-png.d.ts 模式 —— **troika-three-text@0.52.4 无自带类型**：package.json 无 types、dist 仅 .js 无 .d.ts → 声明 `Text extends Object3D` + 项目用到属性 + sync/dispose）。④**`test/labels-layer.test.ts`** **17 测**（parseLabels 8：合法解析/非数组/元素非对象/缺字段/非法 kind/continent 非 string|null/NaN·Infinity 非有限数/真实结构字段全覆盖 ｜ labelWorldPosition 5：x-z 与 project 多锚点同源/陆地贴地面(>海面)/大洋贴海面(max 抬海底负值到0)/heightOffset 陆地大洋均叠加/海面非0贴合 ｜ LABEL_STYLE 3：非负+合法 hex+锚点居中 ｜ labelFontUrl 1）。⑤**`src/data/assets.ts`**（✅ 允许 src/data/** Label 部分）—— 新增 `parseLabels(input)` 纯函数（逐条严格校验类型，与 parseMeta 同风格非法抛错）+ `isLabelKind` type guard + `LABEL_KINDS` + `loadLabels()` fetch（dataUrl('labels.json')）；import 加 Label。⑥**`src/three/Scene.tsx`** 挂 `<LabelLayer>`（labels 独立 useState+useEffect 加载，**失败仅 console.error 不阻塞 Terrain/Ocean**；assets 就绪后渲染，labels 就绪再挂 LabelLayer）。⑦删 Task01 占位 `src/three/labels/.gitkeep`。**范围切割兑现**：Task 14 = 渲染层（11 标签全显、中文无缺字、锚点对齐、4K 60fps）｜ Task 15 = collision.ts 优先级视口 AABB 剔除 + useLabelCollision + LOD 联动（订阅 cameraZoom/qualityTier labelDensity）—— §6.5「优先级视口碰撞剔除+LOD」整段留 Task 15，M4 MVP 11 条不重叠无需碰撞，验证渲染层即可。**锚点 Y 决策（max 逻辑，关键）**：SPEC §6.5 字面「project+地形高度查询 y」对大洋=海底负值，锚点落海底会被半透明 Ocean 覆盖不可见（Task06 渲染序 Ocean 后绘读 Terrain 深度）→ `max(地面, seaLevelY)` 让陆地贴地 y>0、大洋贴海面 y=0，地理+视觉均正确；属 LabelLayer 内部纯函数锚点计算（可单测），**非改 project/投影契约/shader/跨边界**，无需确认。**既有契约不退化**：渲染顺序（Terrain 先绘写深度/Ocean 后绘）/光照/投影/store cameraZoom 切片/SandboxControls/AdaptiveQuality 全保留；types.ts Label 不动（Task13 已补 continent）；troika Text 透明物体自动透明 pass 渲染不破坏 Ocean 渲染序。**模块数 601→603**（+LabelLayer/labelLayout；troika 及依赖 bidi-js/troika-three-utils/troika-worker-utils/webgl-sdf-generator 进 bundle）；bundle 1.1MB→1.24MB / gzip 313→355KB（troika SDF 库固有成本，SPEC §3 line117 已计划）。**踩坑（react-hooks/use-memo）**：`useMemo(labelFontUrl,[])` 传命名函数引用 → v7 `react-hooks/use-memo`「Expected inline function expression」→ 改 `useMemo(()=>labelFontUrl(),[])`。**踩坑（troika 无类型）**：dist 无 .d.ts/package.json 无 types → tsc「无声明文件」→ 建 ambient shim（同 fast-png.d.ts）。**验收**：11 标签渲染（parseLabels 解析+troika Text zhName+map-zh.woff2 Task12 charset 全含 Task13 无缺字断言）✅ ｜ 锚点同源（labelWorldPosition 5 测）✅ ｜ build(603mod 437ms)/lint 过 ✅ ｜ **268 测全绿（251→268，+17）** ✅。⚠️ **dev 视觉留人工 Review**（agent 无浏览器，Task 惯例）：① troika 实际渲染 11 条中文无缺字（woff2 字形覆盖）② 锚点位置（七大洲贴陆/四大洋贴海面、中文竖直）③ 描边字可读性/字号/色观感 ④ 4K 60fps ⑤ 浏览器 console（troika worker 字体加载/SDF 生成/woff2 解析/无 error）⑥ sync 异步首次渲染延迟；`docs/screenshots/M4.png` 待 Task 15 碰撞+LOD 后归档。**M4 进度：3/4**。

- **Task 13（2026-06-17）**：大洲/大洋标签数据 pipeline（M4 第二 Task）。落地 SPEC §6.5 / §12.4「labels.json = {id,zhName,kind,continent,lon,lat,priority}」。**三文件新增 + 两文件改**：①**`scripts/data-pipeline/lib/labels-data.mjs`**（核心，纯硬编码数据 + 纯函数，pipeline 与 vitest 共用，照 Task 12 charset 同构模式）—— `CONTINENT_LABELS`（七大洲锚点）+ `OCEAN_LABELS`（四大洋锚点）各含 {id,zhName,lon,lat,priority}，`buildLabels()` 注入 kind/continent 返回 11 条完整对象（字段顺序 {id,zhName,kind,continent,lon,lat,priority} 对齐 SPEC）。②**`scripts/data-pipeline/4-labels-zh.mjs`**（CLI，`pnpm gen:labels`，照 0-synthetic-dem.mjs 风格）→ `public/data/labels.json`。③**`public/data/labels.json`**（产出，11 条：7 大洲 kind=continent/priority 100/continent=自身 id；4 大洋 kind=ocean/priority 80/continent=null）。④**`test/labels.test.ts`** **17 测**（数据完整性 11 条全字段/id+zhName 唯一 ｜ 优先级 大洲100>大洋80 ｜ continent 语义 大洲=自身id/大洋=null ｜ **无缺字 zhName 与 charset.mjs CONTINENT/OCEAN_NAMES 集合相等 + 每汉字在 DEFAULT_CHARSET** ｜ 锚点 大洲真实地理 bbox + 大洋 isLand=false ｜ project 投影到平面）。⑤`src/data/types.ts` Label 补 **`continent: string|null`**（大洲=自身 id/大洋=null，落地 SPEC §6.5 格式；types.ts 注释原说「M4 填充完整字段」，Label 类型 Task 14 才消费，src/three/labels 仅 .gitkeep，非改既有契约）。⑥`package.json` 加 `gen:labels`。**范围切割**：七大洲 + 四大洋是**固定地理常识**（中文名与 Task 12 charset 同源、锚点经纬度、优先级均确定），**无需 Natural Earth / ISO_A3 join**（国家 join 留 Phase 2 M8 Task 25）——故纯硬编码数据 + 纯函数可单测，与 Task 12 charset 同理。**优先级**（§6.5「大洲>大洋>大国>小国」）：大洲 100 / 大洋 80，为 Phase 2 国家（大国~60/小国~30）留数值空间。**锚点策略**（§10 同理）：人工代表性中心点**非几何质心**（避免质心落海/偏移）——亚洲(95,45)中亚高原/欧洲(15,52)中欧/非洲(20,5)刚果/北美(-100,45)中部/南美(-60,-15)中部/大洋洲(135,-25)澳中/南极(0,-82)内陆；太平洋(-160,0)/大西洋(-30,0)/印度洋(80,-20)/北冰洋(0,85)。**踩坑（合成 mask isLand 局限）**：原想用 continents.mjs `isLand` 断言「大洲落陆/大洋落海」（强力可编程验收）——亚洲(95,45)等 6 大洲 + 4 大洋全过，**唯独欧洲(15,52) isLand=false 误判**。根因：continents.mjs 的 Eurasia 是「欧亚拼合多边形」，射线法在**中东锯齿边界**（里海/中东凹凸 lat46-57 经度30-58 区域）对中欧点误算交点（水平射线向右必经锯齿区多算穿越 → 判外部）；(15,52) 真实是陆地，是 mask「可辨认级」简化局限非锚点错误。**诚实修复**：大洲锚点改用**真实地理 bbox** 验收（7 个 bbox，鲁棒、不依赖合成 mask 脆弱性），大洋锚点保留 `isLand===false`（开阔海域中心判海可靠，4 条全过）。**既有契约不退化**：build **601 modules 不变**（Task 13 纯 scripts + public 产物 + test + types 类型修改，subset-font 同样不进 vite 前端 bundle，不影响 tsc/vite）；types.ts Label 补 continent 是落地 SPEC 已定义格式（非改架构）。**衔接**：Task 14 LabelLayer troika 消费 labels.json，锚点 = `project(lon,lat)` + CPU 高度查询表 y；中文名 Task 12 charset 已全含（17 测「每汉字在 DEFAULT_CHARSET」断言兑现无缺字）。**验收**：11 条全字段 ✅ / priority 大洲>大洋 ✅ / 无缺字（charset 同源）✅ / 锚点合理（大洲 bbox + 大洋落海）✅ / build+lint 过 ✅ / **251 测全绿（234→251，+17）** ✅。⚠️ **dev 视觉留 Task 14**（LabelLayer troika-three-text 实测 labels.json 11 条中文渲染、锚点位置、4K 60fps）。**M4 进度：2/4**。

- **Task 12（2026-06-17）**：字体子集化 pipeline（M4 首 Task）。落地 SPEC §6.5 / §12.5「构建期 subset-font 提取仅所需字形 → map-zh.woff2（数 KB~数十 KB）」。**四文件新增 + 三文件改 + 两 .gitkeep 删**：①**`scripts/font-subset/charset.mjs`**（核心，纯函数可脱离 DOM 单测，被 pipeline + vitest 共用）—— 导出 `CONTINENT_NAMES`（七大洲固定中文名 亚洲/欧洲/非洲/北美洲/南美洲/大洋洲/南极洲）+ `OCEAN_NAMES`（四大洋 太平洋/大西洋/印度洋/北冰洋）+ `BASE_CHARS`（ASCII 字母数字 + 常用中英标点 ·°— 空格 半/全角括号，作「无缺字」保险，M4 标签纯中文但留兜底）+ `collectCodepoints(names,extra)` 纯函数（收集 + 去重 + 升序 code point 数组）+ `codepointsToString`/`buildCharsetString`（subset-font 要求 text 为 string）。**七大洲四大洋中文名是固定常识**（不依赖 Task 13 labels.json 数据 pipeline），故 M4 charset 完全确定；Task 13 产出 labels.json 后可扩展 names 重新 `gen:font`（pipeline 接收任意 names 列表，已单测验证可扩展性）。②**`scripts/font-subset/fetch-source.mjs`**（下载源字体，`pnpm gen:font:fetch`，GEBCO 同款模式）—— 从 google/fonts 官方仓库下载 **NotoSansSC[wght].ttf（OFL 可变字体，16.9MB）** 到 `raw/`（.gitignore），下限 10MB 校验防 CDN 截断，已存在则跳过。③**`scripts/font-subset/subset.mjs`**（子集化 pipeline 主脚本，`pnpm gen:font`）—— 读 raw/ 源字体 + charset → **subset-font（harfbuzz hb-subset wasm）**提取所需字形 → 写 `public/fonts/map-zh.woff2`，体积 >100KB 退出非零。**关键 API 细节**：subset-font 强制 text 为 string（`for(const c of text) c.codePointAt(0)` 按 code point 迭代，正确处理 surrogate pair），故 charset 传 **string 不传 codepoint 数组**（传数组抛 `must be given as a string`）；源字体是可变字体故 **`variationAxes:{wght:400}` pin 固化为 Regular 静态子集**（去 glyf variation deltas → 体积最小且确定，22.1KB vs 不 pin 会偏大）。④**`scripts/font-subset/README-下载说明.txt`**（人工指引，GEBCO 同款）。⑤**`public/fonts/map-zh.woff2`**（产出，**22680 字节 ≈ 22.1KB**，远 < 100KB 验收线；92 字形：16 个大洲大洋去重汉字 + ASCII+标点）。⑥**`test/font-subset.test.ts`** **13 测**（charset 数量/固定值 7+4 / 无缺字根因断言：默认 charset 覆盖七大洲四大洋所有名字形 / BASE_CHARS 含 ASCII+度+中点+空格 / collectCodepoints 去重+升序+可扩展自定义 names / codepointsToString 与 collectCodepoints 按 code point 1:1 同源 / buildCharsetString=DEFAULT_CHARSET 一致 / woff2 体积<100KB+非空+magic「wOF2」=0x77 0x4F 0x46 0x32；**woff2 校验用 `it.skipIf(!existsSync(WOFF2))` —— CI/无源字体环境自动跳过，本地产出后即校验**）。⑦`.gitignore` 加 `scripts/font-subset/raw/`（源字体 16.9MB 不进 git）。⑧`package.json` 加 `gen:font:fetch`/`gen:font` script。⑨删除 Task 01 占位 `scripts/font-subset/.gitkeep` + `public/fonts/.gitkeep`（目录已有实际内容）。**踩坑（GitHub raw 国内大文件慢）**：首次 `gen:font:fetch` 下载 16.9MB 从 raw.githubusercontent.com 在国内带宽下偏慢（HEAD 秒回但大文件正文传输慢，实测约 3 分钟才完成），最终成功未换源；若反复超时可改 jsDelivr 镜像（`cdn.jsdelivr.net/gh/google/fonts@main/ofl/notosanssc/NotoSansSC%5Bwght%5D.ttf`，国内有 CDN 节点更快）。**踩坑（MSYS 路径转换）**：曾尝试 `taskkill /PID` 杀卡住的下载进程，Git bash 把 `/PID` 误转为 `C:/Program Files/Git/PID` 路径 → 失败；但任务最终自然完成（exit 0）无需 kill，下次用 `//PID` 或 `MSYS_NO_PATHCONV=1` 规避。**切割兑现**：Task 12 = 字体子集化**工具** + charset（七大洲四大洋字形全集，固定常识）；Task 13 = 大洲/大洋标签**数据** pipeline（labels.json 含 zhName/lon/lat/priority/kind）。**Task 13 中文名落地后理论上可回 `gen:font` 扩展 charset，但实际 M4 大洲大洋名 Task 12 已全含，Task 13 衔接零缺字**（国家名是 Phase 2 M8）。**既有契约不退化**：src/ 零改动（Task 12 纯 scripts + public 产物 + test + 配置，前端消费 woff2 留 Task 14 LabelLayer troika SDF），build 601 modules 不变（subset-font 是 scripts 构建期依赖，CJS 不进 vite 前端 bundle，不影响 tsc/vite）。**验收**：woff2 22.1KB<100KB ✅ / 无缺字（charset 92 字形覆盖七大洲四大洋全名 + subset-font 契约保留传入 code point + GSUB layout closure）✅ / build+lint 过 ✅ / **234 测全绿（221→234，+13）** ✅。⚠️ **dev 视觉留 Task 14**（LabelLayer troika-three-text 实测 map-zh.woff2 中文渲染是否正确无缺字、4K 60fps）；OFL 字体署名入口留 Task 18（M5 数据来源/许可弹窗）。**M4 启动：1/4**。

- **Task 11（2026-06-17）**：AdaptiveQuality 分档（M3 末 Task，M3 收官）。落地 SPEC §8 / D18「FPS/设备探测 → 三档自动降级 + 跨阈值不抖动 + 手动覆盖」。**四文件新增 + 五文件改**：①**`src/config/quality.ts`**（扩展，✅ 允许边界）—— `qualityConfigs` 每档补 **`terrainEffects` 5 开关**（高全开 slope1/noise1/coast1/rim1/detail0.3 ｜ 中 noise0.5/rim0.5/detail0.2 ｜ 低 noise0/rim0/detail0 坡度0.5 海岸线保留维持辨识）+ 新增 **`qualityAdaptive`** 滞回阈值（`fpsWindowSec:1` / `downgradeFps:45` / `upgradeFps:57` [>降档形成滞回带] / `cooldownSec:2` / `sustainedWindows:2`）+ `QUALITY_TIER_ORDER:['low','medium','high']` + 导出 `LabelDensity`/`QualityAdaptiveConfig` 类型。②**`src/three/effects/qualityState.ts`**（**新，纯函数，可脱离 DOM/drei 单测**，照 cameraState.ts 同构模式）—— `initialTierFromGpu(gpuTier,isMobile)`（useDetectGPU tier≥2 桌面→high / 移动端封顶 medium / undefined→medium 保守）/ `hysteresisDecide(current,fps,cfg)`（<降档阈值下一低档、>升档阈值下一高档、**滞回带内不动**、**只移一档不跳档**）/ `avgFpsFromDeltas`（帧数/总时长）/ **`stepFpsWindow(state,fps,cfg)`** 状态机（**三重防抖动核心**：持续命中 `candidateStreak≥sustainedWindows` + 冷却 `evalCount-lastSwitchEval≥ceil(cooldownSec/windowSec)` + 滞回带，候选方向变化重置计数）+ `clampDpr(deviceDpr,dprMax)`（4K dpr 钳制，无效值回退1）。③**`src/three/effects/AdaptiveQuality.tsx`**（**新，R3F 胶水**，渲染 null 纯副作用）—— `useDetectGPU()`(drei)→初定档写 store；`useFrame` 累加帧 delta 到滑窗（**delta>0.25s 视为不连续→重置窗口不计入**，防后台切回误判降档），窗口满→`avgFps`→`stepFpsWindow`→档变则 `setQualityTier`；dpr 经 `useThree(setDpr)`+`clampDpr(window.devicePixelRatio,dprMax)` 应用（qualityTier 变化时 effect 触发）；**手动覆盖** `qualityTierOverride` 非 null → `setQualityTier(override)` + useFrame 内 `getState().qualityTierOverride!==null` 早返回**冻结自适应**，恢复 null 立即对齐探测档。④**`src/state/store.ts`**（✅ 允许）加 `qualityTierOverride:QualityTier|null`(默认 null=自动) + `setQualityTierOverride`；`qualityTier` 切片保留为**生效档**（渲染层订阅）。⑤**`src/three/ocean/Ocean.tsx`** + ⑥**`src/three/terrain/Terrain.tsx`**（**对 ocean/terrain 仅改 .tsx 组件，不动 GLSL shader 主体 / 不动 oceanMaterial/terrainMaterial 定义文件**）—— 订阅 `useStore(qualityTier)`，材质 `useMemo` 加 qualityTier dep，经**既有工厂 opts** 传分档值（Ocean `{waveCount:qualityConfigs[tier].oceanWaves}` / Terrain `qualityConfigs[tier].terrainEffects`）→ 材质重建**同源 shader 仅 uniform 值变**（three 按 shader 源码 + defines 缓存 program，同源复用不重编译），**兑现 M2 Task 07/08 预留的「M3 改 uniform value 不动 shader」钩子**。⑦**`src/three/Scene.tsx`** 挂 `<AdaptiveQuality/>`（§4.3 管线首项，Ocean/Terrain 订阅 qualityTier 前）。⑧**`src/three/terrain/terrainMaterial.ts`** `TerrainEffectOpts` 类型由 `Partial<typeof TERRAIN_EFFECTS>`（`as const` 字面量 `1`/`0.3`）改为 number 字段（**纯类型修改非 shader 主体**，承接 qualityConfigs 分档 number 值，M2 预留 opts 衔接处类型修正）。⑨**`test/quality.test.ts`** 38 测。**踩坑（tsc 类型）**：`TerrainEffectConfig`(number) 不 assignable 到 `Partial<typeof TERRAIN_EFFECTS>`(字面量) → 改 opts 类型为 number（非 cast，根因修复）。**踩坑（react-hooks）**：useFrame 内读 override/tier 用 `useStore.getState()`（zustand 非订阅读，避 stale closure + 免额外 selector 订阅），init effect 用 `initedRef` 守卫防 useDetectGPU tier 异步更新重复初始化。**既有契约不退化**：渲染顺序 / 光照 / 投影 / store cameraZoom 切片 / SandboxControls 输入 / ocean uTime 流动（matRef 同步）全保留；默认档 high = M2 行为（store 初值 high + Ocean/Terrain 首渲染 high 材质，AdaptiveQuality 探测后纠正）。**模块数 62→601**：首处 `import{useDetectGPU}from'@react-three/drei'` 拉入 drei 依赖图（SPEC §3 计划内，drei 专为 M3 Task 11 useDetectGPU 装的）；bundle 1.1MB/313KB gzip（three+drei 固有成本）。**手动覆盖无 UI**：M3 边界禁 `src/ui/**`，override 经 store API 暴露（`useStore.getState().setQualityTierOverride('low')`），覆盖 UI 入口留 M5 Task 18 HUD。**验收覆盖**：掉帧降档 / 跨阈值不抖动（持续命中+冷却+滞回带）/ 4K dpr 受控（clampDpr）/ 手动覆盖（store+冻结）均有纯函数单测；共 **221 测全绿**（183→221）build(601mod 452ms)/lint 过。⚠️ **dev 真实环境需人工 Review**：① 真实 GPU 探测初档是否合理（useDetectGPU tier 估计）② 真实掉帧（如开 DevTools throttle / 拖窗到 4K）是否在 ~3-4s 内降档且不抖动 ③ 4K 屏 dpr 是否真钳到 2 ④ 手动覆盖（console setQualityTierOverride）是否锁定冻结自适应 ⑤ 浏览器 console（setDpr / useDetectGPU / 无报错）；`docs/screenshots/M3.png` 待人工 `pnpm dev` 归档（M3 三 Task 视觉汇总：受限相机+输入+分档）。**M3 收官：3/3 ✅，MVP 3/5**。

- **Task 10（2026-06-17）**：输入手势适配（M3 第二 Task）。把 Task 09 内置在 `SandboxControls.tsx` 的 pointer/wheel DOM 绑定抽成 **InputAdapter 抽象层**，落地 SPEC §9 / D20「鼠标+触控板为主，预留触屏」。新增三文件 + 改两文件：①**`src/three/camera/inputAdapter.ts`**（核心抽象，纯 TS 可单测）—— `CameraInputHandlers`（语义意图 `onPan(dx,dy)`/`onZoom(factor)`）+ `InputAdapter` 接口（`attach(el,handlers)→detach`）+ **`wheelToZoomFactor` 纯函数**（**鼠标 vs 触控板 deltaY 量级差异核心**：`deltaMode` 归一—— LINE(1)×16 / PAGE(2)×400 归到 PIXEL(0) 像素量级；`ctrlKey=true` 触控板 pinch 走独立 `pinchZoomFactor`；**Task 09 回归不变量** PIXEL+无 ctrlKey = `exp(deltaY·wheelZoomFactor)` 严格保留）+ `createMouseTrackpadAdapter`（鼠标/触控板统一：pointer 主键拖拽→pan、wheel→zoom；pointer 事件天然覆盖触屏单指 pan，pointerType=touch 同源）+ `createTouchAdapter`（**stub 预留**，Phase 2 / Task 30 多点触控 pinch/tap，返回 no-op attach，抽象完整未来替换不动控制器）。②**`src/hooks/useCameraInput.ts`**（输入 hook，ROADMAP M3 允许 `src/hooks/**`）—— `useState` 懒初始化默认 adapter（引用稳定）+ `latest` ref 透传 handlers（调用方无需 useCallback 稳定化）+ attach effect 依赖 `[el,active]` 全程绑一次。③**`src/three/camera/SandboxControls.tsx`** 改造：删内联 useEffect 事件块（pointerdown/move/up/cancel/wheel 全移走），`onPan`/`onZoom` 用 `useCallback([])`（读 goal.current ref，稳定）→ `useCameraInput(gl.domElement,{onPan,onZoom})`；**状态机/约束/clamp/阻尼/useFrame/节流写 store/初始化 store 全部不变**（Task 09 契约零退化）。④`config/camera.ts` 加 `pinchZoomFactor:0.01`（pinch deltaY 量级小故系数大于 wheelZoomFactor 0.001）。⑤`test/inputAdapter.test.ts` **27 测**（fake-element 集成 —— vitest node 环境无 DOM，自建 addEventListener/removeEventListener/dispatch stub + plain event 对象驱动，**不依赖 jsdom / 不加 npm 依赖**）：wheelToZoomFactor 11 测（PIXEL Task09 回归 / LINE×16 / PAGE×400 / pinch ctrlKey 独立系数 / 方向）+ mouse-trackpad adapter 13 测（拖拽→pan 增量 / 未按下不 pan / 非主键不 pan / up&cancel 结束拖拽 / 累积增量各自上报 / wheel→zoom 量级归元 / wheel pinch 用 pinchZoomFactor / wheel LINE 归一 / setPointerCapture+releasePointerCapture / detach 移除监听计数归零）+ touch stub 3 测。**踩坑**：⑴ **`react-hooks/refs` v7 禁止 render 期读 ref.current**（Task 09 备记的坑再现）—— 首版 `const active = adapter ?? owned.current`（owned 是 useRef 懒初始化）在 render 期读 ref.current 报错 → 改用 **`useState(() => adapter ?? createMouseTrackpadAdapter())` 懒初始化**（读参数 `adapter` 非 ref，合法；引用稳定且避开规则；代价：adapter 首次确定后不变，但 adapter 本就该是稳定引用）。⑵ `DELTA_PIXEL=0` 常量声明未用（PIXEL 是默认无操作分支）→ 删除常量、注释保留（tsc6133 + eslint no-unused-vars 双报）。**既有契约不退化**：状态机 / clamp 全套 / 阻尼 / computeCameraPosition / store cameraZoom 切片签名 / SandboxControls 挂载（Scene.tsx 未动）全保留；mouse-trackpad 行为 = Task 09（PIXEL 回归单测保证）。共 **183 测全绿**（156→183）build(62 modules 295ms)/lint 过。⚠️ **dev 手感（滚轮 vs 触控板双指滚动 zoom 速率是否一致、触控板 pinch ctrlKey 是否真触发 pinchZoomFactor、拖拽 pan 方向直觉、阻尼跟手）+ 浏览器 console（wheel preventDefault / pointer capture / setPointerCapture 权限）需人工 Review**（agent 无浏览器，Task 04/09 惯例）；触屏真实多点 pinch 留 Task 30（M11，TouchAdapter stub 替换为真实实现）。**Task 10 切割兑现**：Task 09 = 控制器内核 + 内置输入；Task 10 = 输入层抽象（状态机/约束/阻尼不变，可控返工）—— 按此切割，SandboxControls 仅替换输入绑定，零行为回归。**M3 进度：2/3**。

- **Task 09（2026-06-17）**：SandboxControls 受限相机（M3 首个 Task）。`StaticCamera.tsx`（Task 04 占位静态倾斜相机，注释明示"M3 Task 09 替换"）→ **删除**，Scene 改挂 `SandboxControls.tsx`。落地 SPEC §6.6 / D13「固定倾斜 + 限 pan/zoom」：①**球面相机状态机**（`cameraState.ts`）——绕目标点 (targetX,0,targetZ) 的球面，相机位 = `target + distance·(cos pitch·sin yaw, sin pitch, cos pitch·cos yaw)`；`pitch=pitchDeg(45°)/yaw=0` 时退化为 Task 04 StaticCamera 同源位置 `(targetX, sin(pitch)·d, targetZ+cos(pitch)·d)` + `lookAt(targetX,0,targetZ)`，**M1→M3 切换无跳变**（单测 sin/cos 同源 <1e-10）。②**约束全纯函数化（SPEC §6.6 边界，可单测）**：`clampTarget`（pan 目标点 `x∈[-1.1,1.1]/z∈[-0.6,0.6]`，与 PLANE_WIDTH=2/HEIGHT=1 比对 = 平面外缘略放宽）、`clampDistance`（zoom.min/max）、`clampPitch`（[pitchMin°45, pitchMax°60]，任意输入含 >90° 翻背面/≤0° 平视仰视 → 全压回区间 → **永不翻转**）、`clampYaw`（±yawRange°15，锁定主朝向 yaw=0）、`distanceToZoom`（距离→归一化 [0,1]，远=0/近=1）、`damp`（帧率无关阻尼 `1-pow(1-damping, delta·60)`，两半帧≈一整帧单测验证）。③**交互（Task 09 内置最小输入）**：主键拖拽 → pan target（拖地图方向：右拖 targetX−、下拖 targetZ− 即向北；灵敏度 = `distance·panSensitivity` 随距离缩放），滚轮 → zoom（`distance *= exp(deltaY·wheelZoomFactor)` 指数步长，向后滚拉远），**pitch/yaw 不绑定输入 → 恒定 45°/主朝向**。④**阻尼状态机**：`current`（阻尼后驱动相机）vs `goal`（输入改它），`useFrame` 每帧 `damp` 三轴（targetX/targetZ/distance）+ `delta clamp≤0.1` 防后台回切大跳；pitch/yaw 每帧 `clampPitch/clampYaw`（输入不改但 clamp 兜底防翻转）。⑤**store `cameraZoom` 切片接入**：useFrame 节流（|Δ|>0.001 才 set，避免每帧触发订阅者）写归一化 zoom；挂载时 `setCameraZoom(distanceToZoom(initialDistance))` 同步初始（store 默认 1 ≠ 实际 ≈0.43）—— 供 M4 Task 15 LOD 联动订阅。**Task 切割决策**：Task 09 = 控制器内核（约束 clamp 纯函数 + 阻尼 + 内置 pointer/wheel）；Task 10 = `InputAdapter` 抽象（鼠标 vs 触控板 deltaY 量级差异 + 触屏 pinch 预留）。ROADMAP 明确此切割，Task 09 直接绑 `gl.domElement` 的 pointerdown/move/up/cancel + wheel 够验证边界，Task 10 把输入层抽象替换（**状态机/约束/阻尼不变，可控返工**）。`camera.ts`（Task 01 骨架已含 pitchDeg/pitchMin-Max/yawRange/panBounds/zoom/fov/initialDistance/damping）补 `panSensitivity:0.0015` + `wheelZoomFactor:0.001`（控制器调参集中 config）。**踩坑（react-hooks/refs）**：`useRef(distanceToZoom(current.current.distance))` 在 useRef **初始化参数读 ref.current** → eslint `react-hooks/refs`「Cannot access refs during render」报错（即使只读初始值）→ 改用 `distanceToZoom(cameraConfig.initialDistance)`（与 initialCameraState 同源常量，不读 ref）；useFrame/useEffect 内改 ref 子属性放行（同 Task 07 ocean uTime、Task 04 camera 经验）。**既有契约不退化**：computeCameraPosition pitch/yaw=0 与 StaticCamera 同源（视角连续）；Scene 渲染顺序/光照/Terrain+Ocean 不动（M2 契约保留）；store cameraZoom 切片签名不变（Task 15 消费）。**纯函数同源单测**：clampTarget/clampDistance/clampPitch/clampYaw/distanceToZoom/damp/computeCameraPosition/initialCameraState 全覆盖；新增 **35 测**（pan 边界 6 + zoom 区间 4 + pitch 锁 6 + yaw 4 + distanceToZoom 7 + damp 5 + 初始态 1 + 相机位置同源 4），共 **156 测全绿**（121→156）build(60 modules 335ms)/lint 过。⚠️ **dev 视觉/手感（拖拽 pan 方向是否符合直觉、滚轮 zoom 速率、阻尼跟手度、pan 到边界是否硬停）+ 浏览器 console（pointer capture / wheel preventDefault / 相机抖动）需人工 Review**（agent 无浏览器，Task 04/05/06/07/08 惯例）；`docs/screenshots/M3.png` 待 Task 11 AdaptiveQuality 后归档（M3 三 Task 视觉汇总）。**M3 启动：1/3**。

- **Task 08（2026-06-17）**：地形水彩 shader 完善（M2 第三/末 Task）。`terrainMaterial.ts` 由 Task 04 基础高度分层升级为**完整水彩 shader**，落地 SPEC §2.2 全五要素 + §2.4 海岸线 + §6.1.3 normal.png 细节 + §2.1 palette 接入：①**坡度强调**（§2.2.3）`smoothstep(0.30,0.65, 1-clamp(N.y))` 陡坡偏暖灰绿（mix mountain 色）仅陆地；②**水彩噪声**（§2.2.2）hash fbm（4 频 valueNoise）调制明度 `(grain-0.5)*0.20` 形成「水彩颗粒」，**无纹理依赖**（纯 GLSL hash，避免新增资产/越界 data 层）仅陆地；③**海岸线 fwidth 等高线**（§2.4）`1-smoothstep(0, fwidth(vWorldY)*2, |vWorldY|)` 在 y≈0 海平面处导数抗锯齿描边（**非额外几何**，SPEC §2.4 明确要求），描边色取 palette.border 暖白；④**软描边轮廓 rim**（§2.2.4）`pow(1-max(dot(N,V),0),2)` 暖白 additive，掠射角偏亮，仅陆地。⑤**normal.png 细节增强**（§6.1.3）：Task 04 注释「normal.png 细节增强留 Task 05/08」明确 deferred→本 Task 兑现。片元采样烘焙法线，**编码契约关键**：`heightmap.mjs:computeNormals` 输出 R=worldX / G=worldZ / B=worldY（均 `n·127.5+127.5`，nx=-dzdx 东向/ny=-dzdy 北向/nz=1 上），故 GLSL 解码 `vec3(nrgb.r*2-1, nrgb.b*2-1, nrgb.g*2-1)` 取 **(r, b, g)** → (worldX, worldY, worldZ)（**非 R=X/G=Y/B=Z 常规切线空间**，因烘焙在地理图像空间且行 0=北→+y=南=世界+Z）；按权重 `uDetailNormal=0.3` blend 进几何法线（`mix(geoN, detailN, w)` + 双方 `if(N.y<0)N=-N` 保 +Y）治「低频地形发软」（512×256 网格 dFdx/dFdy 法线过粗，4096 烘焙法线补亚像素细节，strength=6 烘焙前放大仅影响方向不影响幅度因 normalize 抹除）。⑥**palette 完整接入 + S 降饱和**（§2.1）：terrainMaterial 分层色由**硬编码 LAYER_COLORS hex → 源自 `config/palette`**（grassland[0]→plain / mountain[0]→mtn / mountain[1]→hill / desert[0]→beach / snow / border→coast&rim），经 `desaturateHex`（S 降 20% 取中值 15-25%）低饱和化。**palette.ts 设计决策（向后兼容）**：`palette` 原值**不变**（ocean Task 07 已据此验证，ocean.test 比较 `palette.oceanShallow` 仍绿），仅**新增** `desaturateHex`（纯 TS HSL sRGB↔HSL 往返，无 three 依赖，config 层保持轻）+ `snow` 色 + `SATURATION_REDUCTION=0.2`。**故 S 降饱和仅作用于 terrain**（Task 08 行语境）；ocean 未动（Task 07 刚验证、不在本 Task 必须项；若要全局统一低饱和是后续小改）。**shader 复杂度开关位（§8/D18）**：5 个 uniform 钩子 `uSlopeEmphasis/uWatercolorNoise/uCoastline/uRimOutline/uDetailNormal`（`TERRAIN_EFFECTS` 高档默认 slope/noise/coast/rim=1、detail=0.3），`createTerrainMaterial(assets, opts?)` 支持 per-toggle 覆盖；**M3 Task 11 经 store 改 uniform value 不动 shader**（与 ocean uWaveCount 同模式；但**未改 quality.ts**——config/quality.ts 不在 Task 08 M2 允许边界 `src/three/{ocean,terrain}/** + palette.ts + Scene.tsx` 内，hook 钩子全在 terrainMaterial 内）。**既有契约不退化**：`shaderWorldY`/`TERRAIN_SEGMENTS`/`terrainLight` 导出未变（terrain.test/Scene.tsx 依赖）；Terrain 保持不透明 `transparent=false/depthWrite=true`（透明渲染顺序契约 Task06/07 不退化，Terrain 先绘写深度 Ocean 后绘关深度）；Lambert（半球+方向光 无镜面）+ 末尾 `pow(linear,1/2.2)` sRGB encode 保留。**纯函数同源单测**：`shaderSlopeFactor`/`shaderRimFactor`/`shaderCoastlineFactor`/`shaderDetailNormalDecode`/`desaturateHex` 导出供 vitest 验证 GLSL 数学 + shader 源码正则防回归（slope/fbm/fwidth/rim/normal 解码/Lambert/gamma 各一句）。**踩坑（8-bit 量化残差）**：`shaderDetailNormalDecode` 平地测试用真实烘焙值 `(128,128,255)/255`——烘焙 `round(n·127.5+127.5)` 把 n=0 落到 128，反解 `128/255·2-1=0.0039` 有 ~0.004 残差（非 bug，8-bit 量化固有，视觉可忽略），故 x/z 容差放宽至 2 位小数、y(B=255 反解精确 1.0) 3 位；新增 46 测（palette 12 + terrain 水彩要素 34），共 **121 测全绿**（75→121），build(56 modules 263ms)/lint 过。⚠️ **dev 视觉（水彩颗粒质感/坡度暖灰绿分层/海岸线亮线/rim 掠射微光/normal 细节是否治发软且不写实）+ 浏览器 console（GLSL 编译/fwidth 导数/normal.png 采样/正常通道顺序）需人工 Review**（agent 无浏览器，Task 04/05/06/07 惯例）；`docs/screenshots/M2.png` 待人工 `pnpm dev` 归档（M2 三 Task 视觉汇总）。**M2：3/3 ✅（MVP 2/5）**。
- **Task 07（2026-06-17）**：Gerstner 海洋 shader（M2 第二 Task）。`oceanMaterial.ts` 由 Task 06 `MeshBasicMaterial` 半透明纯色占位升级为**自定义 ShaderMaterial**，落地 SPEC §6.2 / D8 全部四要素：①**Gerstner 波**（≤5 个，顶点位移 + GPU Gems 1 Ch.1 解析法线：累积 Binormal B/Tangent T → `cross(B,T)` → `if(N.y<0)N=-N` 保 +Y 朝上；GLSL1 `const int MAX_WAVES=5` 定长 uniform 数组 + `for+break` 按 `uWaveCount` 截断；水平位移加本地 x/世界 X、本地 y-=offZ/世界 -Z、本地 z+=dispY/世界 Y）；②**菲涅尔** `pow(1-max(dot(N,V),0),3)` 掠射偏亮青绿 `#BFEDE8`（`vViewDir` 在 vertex 算传 fragment——`cameraPosition` 是 three.js **vertex-only** 内建 uniform，fragment 无）；③**深浅渐变** per-pixel：同源 R32F heightmap 采样 → `terrainY=h·scale+offset`（Task 03 契约）→ `depth=clamp(-terrainY/uMaxDepth,0,1)` → `mix(oceanShallow#7FC4C0, oceanDeep#2E6E73)`（`uMaxDepth=metersToWorldY(2500)=0.0625`；`vHeightUv` 用**位移前**世界坐标算 → 海岸线地理固定，不受波浪水平位移影响）；④**流动**：`uTime` uniform 每帧累加驱动波相位 `w·dot(D,p)+speed·uTime`。**波数开关位（SPEC §8/D18，M3 预留）**：`uWaveCount` uniform + `buildGerstnerWaves(count)`，`count<=0` 降级为 1 个 Q=0 正弦波（§6.2.1「低档减为正弦」）；M2 默认高档 `qualityConfigs[defaultQualityTier=high].oceanWaves=5`，**M3 Task 11 AdaptiveQuality 仅改 uniform value 不动 shader**。**透明渲染顺序契约（Task 06 风险验证#1）保持不退化**：`transparent=true/depthWrite=false/depthTest=true(默认)/DoubleSide/renderOrder=1`（ShaderMaterial 接管材质主体但透明属性不变）。**体积感（§6.2.5）由 Gerstner 波峰/波谷振幅自然提供**（海平面 y=seaLevel=0 不变，不破坏 R3 契约；波幅和<0.05、单波<0.01 世界 Y，≪地形起伏±0.16，柔和浪涌不刺穿陆地）。**踩坑（react-hooks/immutability）**：`useFrame` 回调直接 `material.uniforms.uTime.value+=delta` 被 eslint-plugin-react-hooks v7 `react-hooks/immutability` 规则禁止（`material` 是 useMemo 返回值被视为不可变，Task 04 camera 同类）→ 先试 `matRef.current=material`（render 期间更新 ref）又被 `react-hooks/refs` 拦 → 最终 **`useRef(material)`+`useEffect(()=>{matRef.current=material},[material])` 同步 + useFrame 经 `matRef.current` 更新**（refs 是可变容器，规则放行子属性突变）。**纯函数同源单测（项目惯例）**：`oceanDepthFactor`（深浅 clamp）、`oceanFresnel`（pow 同源）导出供 vitest 验证 GLSL 数学；shader 源码正则断言防回归（`uWaveCount`/`dispY`/`pow(1.0 - max(dot`/`clamp(-terrainY`）。`OCEAN_SEGMENTS=256×128`（Task 06 已为 Gerstner 预留，最短波长≈0.12 世界单位→≈15 格/波长平滑）。新增 **23 测**（Gerstner 波参数各档/方向单位化/振幅递减/波幅尺度、深浅渐变 5 点、菲涅尔 4 点、uniform 波数开关/颜色取自 palette/uTime 初始 0/uMaxDepth/heightmap 复用/shader 源码正则），共 **75 测全绿**（52→75），build(56 modules 295ms)/lint 过。⚠️ **dev 视觉（Gerstner 浪涌/菲涅尔微光/深浅渐变实际观感）+ 浏览器 console（GLSL 编译/运行时、uniform 数组上传、解析法线方向）需人工 Review**（agent 无浏览器，Task 04/05/06 惯例）；`docs/screenshots/M2.png` 待 Task 08 水彩完善后归档。**M2：2/3**。
- **Task 06（2026-06-17）**：透明渲染顺序与海洋几何（M2 首个 Task）。新建 `Ocean.tsx`+`oceanMaterial.ts`：与地形同尺寸(PLANE_WIDTH×PLANE_HEIGHT)平面铺海平面 `y=metersToWorldY(seaLevelMeters)`(=0)，`rotation[-90° X]` 同 Terrain。**SPEC §4.3 透明渲染顺序修正点落地**：Terrain 不透明先绘写深度 → Ocean 透明后绘关深度写入；**关键机制 Ocean `depthTest=true`(MeshBasicMaterial 默认)读 Terrain 已写深度 → 陆地(y>0)遮挡海洋、海床(y<0)被半透明海洋覆盖（海洋不穿地形）**；`renderOrder=1` 保险（Three.js 本已按 transparent 标志自动后绘透明物体）。Task 06 范围切割：`MeshBasicMaterial` 半透明纯色(`oceanShallow #7FC4C0` opacity0.7 DoubleSide)占位，半透明叠加在 terrainMaterial 海床占色(y<0 分支)之上；**Gerstner/菲涅尔/深浅渐变/流动 → Task 07 oceanMaterial.ts**；海平面 y=精确 seaLevel(=0)，Task 07 按 §6.2.5 略低调体积感。**模块拆分（踩坑）**：Ocean.tsx 同时导出组件+常量触发 `react-refresh/only-export-components` lint error → 照 terrain 同构(`Terrain.tsx` 组件 + `terrainMaterial.ts` 常量/函数)拆为 `Ocean.tsx`(只导出组件) + `oceanMaterial.ts`(常量/函数，注释标 Task 07 扩展为 Gerstner 自定义 shader)。`OCEAN_SEGMENTS=256×128`(为 Task 07 Gerstner 顶点位移预留密度，Task 06 平面无位移)。10 新测(`ocean.test.ts`)：几何顶点数/尺寸、渲染顺序契约(transparent/depthWrite/renderOrder/opacity/MeshBasicMaterial 落地 depthTest=true/颜色=palette)、海平面 Y(Task03 契约同源 <1e-9)；共 **52 测全绿**(42→52)，build(54 modules 279ms)/lint 过。⚠️ dev 视觉(海洋是否真不穿地形、半透明纵深观感)**需人工 Review**(agent 无浏览器，Task 04/05 惯例)；`docs/screenshots/M2.png` 待 Task 08 水彩完善后归档。**M2 启动：1/3**。
- **Task 05（2026-06-17）**：M1 闭环验收。**修复 Task 02b 遗留**：真实 GEBCO 产物(4096×2048)替换合成 DEM 后，`test/assets.test.ts` 的 meta 解析测试仍硬编码旧合成值(1024×512/-5000/6500) → 改为 **round-trip**（解析值 = 真实 meta 原值，数据源无关，Task 02b 可插拔契约兑现）。**新增「真实 GEBCO DEM 回归」11 测**：6 陆地 + 4 海洋代表点高程符号正确（陆地用明确内陆点避开海岸双线性跨海——纽约 -74,40.7 落 -0.5m 近海已弃用，换北美中部 -98,41）；全图极值断言 `maxM>7000 & minM<-9000`，基于 elevationMin/Max **硬上下界物理严格区分真实 vs 合成**（合成 min-5000/max6500 物理上触不到）。**⚠️ 关键数据发现**：GEBCO 4096×2048 降采样(~9.8km/px)后珠峰区最高点仅 **7628m**（非 8848），是邻域平均的分辨率损失、**非数据错误**；minM=-10000（海沟 clamp 到 elevationMin，raw16=0）。**验收**：42 测全绿(31→42，修1+增11)、`pnpm build` 通过(52 modules, 322ms)、lint 无错。M1 五条验收全覆盖：顶点数/最大高差(terrain.test)、project()(13测)、CPU/GPU 误差<1e-4(实<1e-9)、真实+合成 DEM 大陆轮廓可辨认(多点回归)、dev 无 console error **需人工 Review**(Task 04 已立惯例，agent 环境无浏览器)。palette 完整接入/S 降饱和留 M2 Task 08，相机/光照 Task 04 已定不动。⚠️ `docs/screenshots/M1.png` 截图待人工 `pnpm dev` 归档。**M1 闭环完成 → M1 ✅(6/6)，MVP 1/5**。
- **Task 02b 闭环确认（2026-06-17）**：Task 02b 此前标 🔄「代码就绪待下载」，**实际闭环已由人工完成并提交**（94f8988 代码 + f6bf8a6 版本同步 2024→2026 + 94cce3c 烘焙产物）：`public/data/` heightmap.png 11.7MB / normal.png 12.1MB（合成版 738KB/333KB），meta.json `source:gebco-2026` 4096×2048 min-10000/max9000。Task 05 跑测试时发现 PROGRESS 落后实际进度（产物已替换、测试 fixture 未同步）→ 本次补记 ✅ 并修测试。**渲染层零改动再次验证**：Terrain/terrainMaterial/projection 全从 meta 读 min/max/尺寸，真实 DEM 替换无需改渲染代码（R1/R3 兑现）。
- **Task 02b（2026-06-16）**：真实 DEM 接入（GEBCO 2026，替换 Task 02 合成噪声为真实地理）。**关键：Task 02 当初设计的「DemSource 可插拔契约」让本次接入极轻量** —— `lib/heightmap.mjs:generateDem()` 数据源无关、**零改动**；只需新增 `lib/real-dem-source.mjs`（同契约）+ CLI `1-gebco-dem.mjs`（`pnpm gen:dem:real`，保留 `gen:dem` 合成版作离线 fallback）。下游 `src/data/assets.ts` 的 `parseMeta()` 只校验 elevationMin/Max 是有限数、`heightToWorldY(h,meta)` 用 meta 的 scale/offset 算世界 Y → **渲染层（Task 03 加载器 / Task 04 shader）零改动**，meta.json 写入新 bounds 即自动适配。**数据源**：GEBCO_2026（公共域，15″ 海洋陆地一体，equirectangular 原生投影，含 bathymetry → 喂 M2 海洋深浅渐变）；Data GeoTiff（8 tiles 各 90°×90° 或单个全球文件，压缩 ~4GB）放 `scripts/data-pipeline/raw/gebco/`（`.gitignore`，不进 git/构建）。**解析**：纯 JS `geotiff@3.0.5`（免 GDAL）`fromFile→getImage→readRasters({width,height,pool:null})` 降采样 + `getBoundingBox()` 读 tile 经纬范围（**不硬编码切分**）→ 逐 tile 降采样写入全局 Float32 栅格；`pool:null` 主线程解码规避 Node web-worker 兼容问题。**输出**：4096×2048 heightmap；elevationMin/Max 固定 **-10000/9000**（覆盖马里亚纳 -10916 轻微 clamp / 珠峰 8848 完整；16-bit 步长 ≈0.29m；固定值→产物确定可复现，优于实测 min/max）。**采样约定 R3 同源**：`getElevation` 抽 `bilinearSampleElev` 纯函数，逐行对齐 `assets.ts:sampleHeight`（像素中心 `floor(sx-0.5)`/经度环绕/纬度钳制）；6 新单测（含经度环绕、纬度钳制、二维中点），共 **31 测全绿**，lint/build 通过。⚠️ **代码就绪，闭环验证需人工**：① 下载数据→② `pnpm gen:dem:real`（验陆地占比回归 ~29%、KNOWN_POINTS 真实绿、ASCII 预览辨认真实七大洲）→③ `pnpm dev` 看真实大陆/山脉。署名：GEBCO 公共域但 best-practice 署名（Task 18 弹窗含入）。SPEC D2/§12.1 已注明实际数据源。
- **Task 04（2026-06-16）**：GPU 顶点位移地形落地。**PlaneGeometry 对齐（关键）**：`rotation[-90° X]` 后本地 `(x,y,0)`→世界 `(x,0,-y)`；**vertex shader 用 `modelMatrix` 反算 worldPos→经纬度→heightmap UV**（`heightUv = (worldX/PLANE_WIDTH + 0.5, 0.5 + worldZ/PLANE_HEIGHT)`，与 `project()`/`sampleHeight` 同源），**刻意绕开 PlaneGeometry 默认 UV**——其 v 方向与 heightmap（row0=北极）相反，直接用会南北颠倒；**位移加在本地 `position.z`**（旋转后 = 世界 Y）。**DataTexture `flipY=false`**（默认，不翻转），`v=0`→row0=北极，与 CPU `sampleHeight` 一致（已核验无颠倒）。**高度解码照搬 Task 03 契约** `worldY = h·uHeightScale + uHeightOffset`；导出纯函数 `shaderWorldY(h, scale, offset)` 供单测验证 R3 同源 <1e-9。**法线用片元导数** `normalize(cross(dFdx(vWorldPos), dFdy(vWorldPos)))` + `if(N.y<0)N=-N` 保 +Y 朝上（`normal.png` 细节增强留 Task 05/08）。**SPEC §2.2 禁 MeshStandardMaterial** → 自定义 ShaderMaterial 自包含 Lambert 光照（`uLightDir` 暖白俯角 50° + 半球光，无 shadow/无镜面），**raw material 不自动 sRGB encode → 末尾手动 `pow(linear, 1/2.2)`**；`THREE.Color`（ColorManagement enabled）传线性值给 uniform。⚠️ **`react-hooks/immutability` 规则禁止对 `useThree` 返回的 camera 做属性赋值**（`cam.fov=` 报错）→ fov 移至 `<Canvas camera={{fov}}>`，StaticCamera 只用方法（`position.set`/`lookAt`/`updateProjectionMatrix`）。**M1 范围切割**：基础高度分层（海岸→平原→丘陵→山脉→雪线，smoothstep 软过渡）已完成；水彩噪声/坡度强调/软描边/海岸线 fwidth 等高线**留 M2 Task 08**。网格 `TERRAIN_SEGMENTS=512×256`（≈13.2 万顶点，M3 接质量档缩放）；静态相机 pitch 45°/距离 2.5/lookAt 中心（M3 换 SandboxControls）；`camera.ts` 加 `fov:45`+`initialDistance:2.5`。5 新单测，共 25 测全绿，build/lint 通过。**⚠️ dev 视觉 + 浏览器 console error 需人工 Review**（无法在此环境启动浏览器验证 WebGL shader 编译/运行时；已用 build 编译 + GLSL 语法/契约审查 + 逻辑断言最大化保证）。
- **Task 03（2026-06-16）**：投影契约 `project(lon,lat)→[x,z]` 锁定（SPEC §5.1）：`x=lon/180×(PLANE_WIDTH/2)`、`z=−lat/90×(PLANE_HEIGHT/2)`（向北 −z），含反函数 `unproject`。**高度解码契约（R3 CPU/GPU 同源，Task 04 shader 必须照搬）**：`worldY = h·uHeightScale + uHeightOffset`，其中 `uHeightScale=(elevationMax−elevationMin)×HEIGHT_EXAGGERATION×WORLD_Y_PER_METER`、`uHeightOffset=elevationMin×HEIGHT_EXAGGERATION×WORLD_Y_PER_METER`，经 `computeHeightUniforms(meta)→{scale,offset}` 暴露；新增艺术常量 `WORLD_Y_PER_METER=1e-5`（峰值 6500m→+0.1625、海沟 −5000m→−0.125；`HEIGHT_EXAGGERATION=2.5` 是 Task 04 视觉旋钮）。解码 `elev=min+h·(max−min)` 与 Task 02 烘焙公式严格互逆。**16-bit heightmap 加载（M1 最高风险点，已正面解决）**：浏览器原生 Image/canvas 会把 16-bit PNG 降为 8-bit（256 级≈45m/步，破坏精度）→ 用 **fast-png 8.0.0** 在浏览器侧逐字节解码 16-bit 灰度→`Uint16Array`（与 Task 02 大端烘焙一致，fast-png 已转本机序）；**three 0.184 不支持 R16_UNORM**（半浮点损精度破坏 <1e-4；整数纹理 R16UI 仅 NEAREST 会阶梯）→ 上传为 **R32F**（`FloatType+RedFormat`，LINEAR，`RepeatWrapping`/`ClampToEdgeWrapping`，`NoColorSpace`），float32 完整保留 16-bit 精度。**CPU 高度查询表**=对同一 Uint16 缓冲双线性采样（经度环绕/纬度钳制/像素中心约定同 Task 02）再 `heightToWorldY`，与 shader 同源。⚠️ 踩坑：**three 0.184 不自带类型且项目未装 `@types/three`**（Task 01/02 仅用 R3F 未暴露）→ 加 `@types/three@0.184.1`；**fast-png 8.0.0 无类型定义**→ 加 `src/types/fast-png.d.ts` ambient shim。引入 **vitest 4.1.9**，测试置于 `test/`（src 外，不进 `tsc -b` 构建，vitest 经 esbuild 转译）；20 单测全绿（project 边界/采样、高度解码与 uniform 一致性 <1e-9、双线性采样器、真实 16-bit PNG 无损解码 + 海洋<海平面<喜马拉雅、R32F 纹理属性）。
- **Task 02（2026-06-16）**：pngjs 的 16-bit 写入路径不可靠（data buffer 未按 16-bit 分配）→ 改**手写 PNG 编解码器**（`scripts/data-pipeline/lib/png-writer.mjs` / `png-reader.mjs`，纯 node `zlib`+`fs`，已移除 pngjs），并用自解码做**整图 16-bit 往返校验**（零像素不一致）。**输出接口（下游 Task 03 加载器 / Task 04 shader 解码须用同公式）**：`raw16 = round((elevMeters − elevationMin)/(elevationMax − elevationMin) × 65535)`；`meta.json` = `{elevationMin:-5000, elevationMax:6500, seaLevelMeters:0, heightExaggeration:2.5, width:1024, height:512, projection:"equirectangular", source:"synthetic"}`。合成 DEM = 手写大陆多边形 mask（`lib/continents.mjs`，6 大洲+格陵兰+主要岛屿；南极洲按纬度特判）裁剪 simplex 噪声（3D 圆柱映射消除经度接缝，固定种子可复现）。陆地占比 ~37%（高于实际 29%，多边形略膨胀，MVP 可接受）。运行 `pnpm gen:dem`（可 `--width/--height`）；heightmap 720KB / normal 326KB，2.3s 完成。⚠️ 顺带修正：Task 01 PROGRESS 原「MVP 进度 1/5」实为 0/5（尚无 Milestone 完成），已校正。
- **Task 01（2026-06-16）**：`@types/proj4` 已废弃（proj4 自带类型），安装后已移除；其余地理库按需装 `@types/{earcut,topojson-client}`。Vite 模板残留已清理（`App.css` / `src/assets` / `public/icons.svg`），`index.html` 改为中文。`Scene.tsx` 暂为背景色占位（Task 04 起填 Terrain）；config 四件套仅骨架，`project()` 实现留 Task 03。
- _（更早：无）_

---

## 维护规则（给 agent）

1. **每完成一个 Task**：把该行状态改为 ✅，填 commit 短 hash 与备注；更新「当前指针」到下一个 Task；在「近期注意事项」追加经验。
2. **开始一个 Task**：把该行状态改为 🔄（in_progress）；开始前用 `git log` 校验与本表一致。
3. **遇阻**：状态改 ⚠️ blocked，备注写阻塞原因与需要的输入，**停下并问我**。
4. **绝不**跳过未 ✅ 的前置 Task；**绝不**修改本文件来"绕过"依赖。
